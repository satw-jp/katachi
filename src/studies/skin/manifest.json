{
  "id": "skin",
  "title": "表面に詰める / 印刷準備 (Surface Patch Packing)",
  "status": "active",
  "version": "0.74.2",
  "createdAt": "2026-07-13",
  "updatedAt": "2026-08-25",
  "params": [
    "count",
    "radiusBase",
    "radiusSpread",
    "k",
    "seed",
    "thickness",
    "minR",
    "maxR",
    "irregularity",
    "coinHoleRatio",
    "gap",
    "attempts",
    "roundK",
    "mode",
    "patchShape",
    "motifPlacement",
    "surfaceGenerationMode",
    "quadDivisions",
    "quadTilingMode",
    "quadSizeVariation",
    "quadConnectionMode",
    "quadConnectionDepth",
    "quadMeshJoinWidth",
    "voronoiSeedCount",
    "voronoiRelaxationSteps",
    "lacePasses",
    "laceMotifPlacement",
    "laceMinScale",
    "laceGap",
    "contactTarget",
    "contactMaxGrowth",
    "contactOverlap",
    "contactReinforcementMode",
    "contactWholeScaleMax",
    "flowerMotifPreset",
    "flowerPetalCount",
    "flowerShowCore",
    "flowerOpening",
    "flowerNeck",
    "flowerCoreSize",
    "flowerCupping",
    "flowerCoreLift",
    "flowerGrowthDifference",
    "flowerExpansion",
    "flowerConnectionMode",
    "flatRingHoleRatio",
    "ringNodeCount",
    "ringTubeR",
    "ringWobbleR",
    "ringWobblePos",
    "coinBulge",
    "coinBulgeBalance",
    "internalStructure",
    "internalDensity",
    "internalRadius",
    "internalRandomness"
  ],
  "related": [
    "cloud-sculpt",
    "foam",
    "rings",
    "pack"
  ],
  "revisits": [
    {
      "date": "2026-08-25",
      "note": "v0.74.2 consolidates the Surface48 / 119.5 mm / 45 degree / scaffold diameter / foot diameter review-condition line into the full-width bottom status pane beside review context. The duplicate condition line is removed from left TOOLS; Case controls and the support legend remain there. This is display-only and changes no geometry, classification, Profile or validation."
    },
    {
      "date": "2026-08-25",
      "note": "v0.74.1 moves the existing one-step and selected-history Undo dock from canvas chrome into left TOOLS. The same callbacks, history counts, keyboard shortcut and multi-step behavior remain unchanged; one/four-view WebGL and picking are no longer covered by Undo UI. Geometry, classification, Support Paint, Profile and validation are unchanged."
    },
    {
      "date": "2026-08-25",
      "note": "v0.74.0 full-width bottom review status pane. The former canvas-fixed v088 exception review title is removed. Review name, Case and actual active viewport direction now update at the left of the bottom pane; selected support classification, downward-ray basis and nearest lower-Surface distance update in the centre; Support Paint autosave state and last save time update at the right. Case controls and diagnosis legend move into left TOOLS, while duplicate support selection and autosave status leave the old UI. A pointer-captured horizontal divider resizes at most once per frame and calls the shared renderer resize so WebGL projection and picking follow; collapse and double-click reset are supported. Backward-compatible bottomHeightPx/bottomCollapsed fields round-trip only through editor layout localStorage/optional draft and never enter Recipe, Print Profile, geometry, routing, validation or output."
    },
    {
      "date": "2026-08-25",
      "note": "v0.73.1 Rhino input refinement. Shift+left-drag now pans the camera in every viewport, including while Support Paint is enabled, and is intercepted before selection, direct manipulation or paint can start. Plain left input remains selection/Support Paint; Axome plain right-drag remains pole-free continuous rotate. Camera state stays editor-draft-only and geometry, classification, routing, Profile, validation and output remain unchanged."
    },
    {
      "date": "2026-08-25",
      "note": "v0.73.0 Rhino-style independent four-view camera input. Orthographic Top/Bottom/Front/Back/Left/Right keep their standard direction and use right-drag pan plus wheel zoom; Axome uses pole-free right-drag quaternion rotate, Shift+right-drag pan, and wheel zoom. Command+right vertical drag zooms every view. Left click/drag remains exclusively selection, direct manipulation and Support Paint; right-camera pointer capture suppresses brush/selection and canvas context menus. Gesture and wheel start activate only the pointer viewport. Camera position/up/target/zoom remain independent and editor-draft-only; title double-click toggles maximize/four-view and Reset restores the direction default around the object centre. Shared clipping, overlays and Support Paint buffers, geometry, classification, routing, Print Profile, validation, 3MF/archive and printApproval=false are unchanged."
    },
    {
      "date": "2026-08-25",
      "note": "v0.72.0 three-pane SKIN authoring workspace. Author observation preserved verbatim: 「左にペインを作って『生成結果を見る』『CLIP XYZ』[1View/4View]等はまとめたい\n各ペインの幅は自分で調整できるようにしたい」. Display-only controls now live in a left tools pane, the shared-scene one/four-view WebGL canvas remains central, and geometry, Dry Web/scaffold, Print Profile, validation, generation and save settings remain in right Properties. Pointer-captured left/right dividers resize at most once per animation frame, double-click resets standard width, collapse buttons preserve the central minimum workspace, and narrow layouts collapse a side. Four-view horizontal/vertical scissor dividers are independently draggable without duplicating scene, geometry or support buffers. Pane widths, collapse state and four-view split ratios round-trip through validated localStorage/editor draft only; Recipe, Print Profile, Profile SHA, routing, validation, 3MF/archive, geometry, classification and printApproval=false are unchanged. No high-resolution generation, archive, Slice, publish or deploy."
    },
    {
      "date": "2026-08-25",
      "note": "v0.71.1 four-view black-canvas boot hotfix. The first afterMutation() could call render() while the demand-driven render scheduler state still sat in its temporal dead zone near the end of main.ts, stopping local SKIN boot with ReferenceError before any viewport draw. The scheduler state now initializes with the other application state before every mutation callback can run. Four cameras, shared scene/scissor rendering, geometry, support classification, Support Paint, Print Profile, validation, recipe and printApproval=false are unchanged. Full focused suite, TypeScript, production build and diff check pass; no archive, high-resolution generation, Slice, publish or deploy."
    },
    {
      "date": "2026-08-25",
      "note": "v088 Support Paint checkpoint D, Rhino-style four-view production environment. Author observation preserved verbatim: 「ライノみたいに４面ビュワーモードがあると作業しやすい\n基本は\n[Top][axome]\n[Front][Right]\nでBottomやback,Leftも切り替えられるといい」. The main canvas switches between one and four views with Top/Axome over Front/Right, per-viewport Top/Bottom/Front/Back/Right/Left/Axome selection, selected frame, axis legend, reset, maximize/double-click and return-to-four. One WebGL renderer scissors one shared scene, geometry and support-site batch through exactly four orthographic cameras; only Axome rotates with the existing pole-free Trackball, while every camera keeps independent zoom/pan. Rendering is demand-driven and four-view DPR is capped at 1.5. Support Paint picking/front visibility/brush use only the active viewport camera; committed shared-buffer colors appear in all four views. Object-coordinate XYZ clipping applies to all views. Optional editorView in the backward-compatible Support Paint draft stores layout, directions and camera poses, but Shape Recipe, Print Profile, routing, validation, 3MF and geometry remain unchanged; printApproval=false. No high-resolution generation, archive, Slice, publish or deploy."
    },
    {
      "date": "2026-08-25",
      "note": "v088 Support Paint checkpoint C: the panel now distinguishes the current editing-preview Surface resolution from the planned print Surface 128, explicitly marked ungenerated. A dedicated low-resolution Worker can build only a Surface 48 support-free BODY diagnosis, then apply the saved normalized object coordinates, surface normals and mm brush radii through the same shared override function without using temporary support-site IDs. The UI compares editing and Surface 48 affected inside/outside counts and reports Auto, manual override, opposite-normal and stored-region-outside counts. Any opposite-normal or out-of-stored-region application fails closed. Focused tests use different low/Surface48 IDs and confirm identical normalized regions and zero penetration. No Surface 128, final geometry, archive or 3MF is generated; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 Support Paint checkpoint B: every committed drag stroke and each Undo/Redo/reset writes one validated draft to localStorage under the exact Shape Recipe SHA-256 and Seed. The panel reports unsaved/saved state and timestamp. 「作業を保存」downloads the same canonical draft JSON; 「作業を読み込む」validates schema, printApproval=false, recipe SHA, Seed and target size before replacing the revisioned session. A mismatch is fail-closed. The draft contains the normalized-coordinate/normal/mm-radius supportPaint document plus brush mode, radius and explicit backface setting. Reloading reuses the shared Worker to restore colors, counts and routing. Existing Shape Recipe and Print Profile schemas remain compatible and unchanged; no geometry generation is performed."
    },
    {
      "date": "2026-08-25",
      "note": "v088 Support Paint checkpoint A: author reports 「ブラシ半径を表示したい」「ペイント中に全部戻ってしまうバグが有る」. Committed history and the active sampled stroke now live in one revisioned paint session. Each begin/sample/commit/cancel advances the revision; a Support Paint Worker response is accepted only when its requested revision equals the current inactive session, so a stale result cannot restore an older overlay. Overlay rebuilds restore the active classification preview. The canvas shows a surface-tangent brush ring at the true object-unit radius derived from mm, a blue-circle/orange-triangle/neutral-Auto centre glyph, and enlarges only the front, unclipped, normal-compatible sites that the stroke would affect. Ring, centre and marker-emphasis BufferAttributes are allocated once and updated in place. The panel states radius and diameter in mm. Shape, automatic classification, routing, Profile, validation and export remain unchanged; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 Support Paint performance-only correction; author observation preserved verbatim: 「ペイントが重くて機能しない」. The old pointermove path projected every support site, appended and revalidated the whole paint document, reapplied every stroke to every site, validated the assignment ledger, rebuilt all support BufferGeometry, refreshed Dry Web targets and recomputed the Profile summary synchronously. The display batch now registers support-site object coordinates and normals in one uniform spatial grid. Picking traverses only ray-neighbour cells and brush preview queries only radius-neighbour cells; clipped, unresolved, duplicate and normal-opposed sites are excluded. Pointer input retains only the latest event and runs at most once per requestAnimationFrame; positions closer than 35% of brush radius (minimum 0.35 mm) are not sampled. During drag only existing color/glyph BufferAttribute ranges are updated. Pointerup commits the sampled dabs as one history transaction, then a dedicated Worker applies all saved overrides, recomputes counts and validates once; stale workers are discarded when a newer stroke commits. Final overlay, Dry Web targets and Profile summary update only after the Worker result. Export/high-resolution application remains in the existing export Worker and uses the same shared supportPaint v1 function. Deterministic 30k fixture measurement: grid build 2.59ms; 100 indexed radius queries 2.88ms versus 5.76ms full scans; 257 candidates/query; shared Worker-core application for 30,000 sites and 10 sampled dabs took 16.04–24.62ms. Runtime status/console record full-site baseline count, average grid candidates, preview p95, Worker compute and pointerup total. Real Case B ten-stroke author timing remains pending. Shape, automatic classification, paint semantics, Profile/SHA, CLI/Worker agreement, validation and archive format are unchanged; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 pole-free 3D review rotation: author observation preserved verbatim: 「ローテイトびゅーするときにある角度で引っかかり裏から見れないので直したい」. Replaced only the main SKIN viewport OrbitControls with Three.js-bundled TrackballControls. Eye and camera-up now rotate by the same quaternion instead of enforcing world-up spherical polar limits, allowing continuous front→top→back→bottom→front dragging and repeated vertical/horizontal 360-degree review without a pole flip. Rotation remains centred on the current object target; mouse zoom, pan, reset and Case A/B top/side/back camera presets remain available. Trackball keyboard mode keys are disabled so application shortcuts and text input cannot change the drag mode. Existing 4px click/orbit disambiguation remains; direct patch dragging and Support Paint still explicitly disable camera control. XYZ clipping remains fixed in object coordinates and camera-independent. Focused tests cover two vertical turns, one horizontal turn, target/radius/up continuity, clipping invariance, zoom/pan/reset availability. This changes no shape, classification, support routing, Profile, recipe, validation, 3MF or archive output; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 viewport-only XYZ clipping observation tool: added a compact monochrome canvas HUD beside the existing view controls, with independent X/Y/Z enable, bbox-driven mm position slider, keep-side reversal, per-axis reset, ALL OFF and RESET. Multiple axes intersect in fixed object coordinates, independent of camera rotation. The same session-only planes clip body mesh/raymarch/beads, Dry Web, fused scaffold, diagnosis surfaces, the common support-site batch, mixed outlines and base footprint. Support marker GPU discard and CPU picking share the same object-coordinate state, so clipped markers cannot be selected or painted; boundary points remain visible. All axes OFF removes local planes and returns to the previous display path. Clipping is not serialized to Shape Recipe or Print Profile and is not passed to classification, routing, validation, mesh generation, 3MF, archive or export. This is an observation window, not approval of classification or printability; printApproval=false. No high-resolution generation, archive, Slice, tag, push, publish or deploy."
    },
    {
      "date": "2026-08-25",
      "note": "v088 support-overlay common-batch rendering fix: author observation and instruction preserved verbatim: 「ある角度でいきなり表示が大きく変わるのはなぜ」「分類や形状は変更せず、角度によって青／オレンジ表示が一斉反転する描画バグを修正してください。」. Both supplied Case A screenshots show identical mixed1139 / inside8636 / outside15906 / unresolved0 / duplicate0 counts, so the discontinuity was presentation-only: separate transparent classification objects with equal render order and depthWrite=false swapped object-sort precedence at a camera-angle boundary. All support sites now share one camera-independent BufferGeometry with per-site hue and glyph attributes and exactly one Points object per visible pass. The opaque front pass uses depthTest/depthWrite; the back pass uses NoBlending with disjoint glyph-specific 3/16 Bayer ranks, so hue is independent of object order. unresolved0 means no red-cross vertex exists. Picking uses the same position/ID/classification arrays and includes back sites only when that pass is visible. Classification counts, support-free Surface ray policy, author overrides, routing, geometry, Print Profile and validation are unchanged. This is not approval of classification or printability; no archive, high-resolution generation, Slice, tag, push, publish or deploy; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 Support Paint production principle: author words preserved verbatim: 「よくはなったけど原理的に恣意性が必要だと思うのでペイント方式などある程度ざっくりと編集できるようにしたい」. This is a production principle, not approval of the whole automatic classification or printability. The support-free Surface downward-ray result remains the automatic draft: plate-visible routes outside/orange/removable scaffold and body-blocked routes inside/blue/Dry Web. Optional Print Profile supportPaint stores normalized support-free-Surface object coordinates, mm and normalized radius, surface normal, stroke order, mode and explicit backface permission. Later strokes win; Auto removes the author override for its region. Painting changes only pre-generation support-site routing, not the body or overhang detection. Normal painting selects only camera-visible front sites, explicit backface painting is opt-in, and a normal-cosine threshold prevents opposite-side penetration. Unresolved remains red and fail closed and cannot be hidden by paint. App, CLI and Worker share one override function; validation records automatic, painted, manual-override and final counts; legacy Profiles remain compatible and canonical Profile SHA includes optional supportPaint. Local Case A author browser review is pending. No archive, high-resolution generation, Slice, print-candidate seal, tag, push, publish or deploy; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 downward Surface-ray routing revision: author observation preserved verbatim: 「outsideになるべきinsideが多い\n下側とかは大体outsideになるべき」. This withdraws the previous rule that routed every support site inside the outer footprint to inside; it is not approval of the whole classification or printability. The shared app/Worker/CLI policy now builds one spatial index from only the support-free body Surface in millimetres and casts each support site downward toward the build plate. A site with no lower body hit is plate-visible / outside / orange / removable scaffold; a site blocked by lower body Surface is body-blocked / inside / blue / Dry Web. Dry Web, scaffold and generated BODY are excluded from classification input. Footprint remains a thin white reference overlay only. Mixed faces remain per-site diagnostics. Only invalid coordinates or ray/index failures are unresolved; unresolved/duplicate remain fail closed. The stable self-hit epsilon is recorded in millimetres in Print Profile and validation. Selecting a marker shows plate-visible or body-blocked, nearest lower-Surface distance and epsilon while circle/triangle/cross and depth-opacity presentation remain unchanged. Review counts and browser screenshots are pending verification. No shape/archive/high-resolution generation, Slice, print-candidate seal, tag, push, publish or deploy; printApproval=false."
    },
    {
      "date": "2026-08-25",
      "note": "v088 support-overlay depth review: author observation preserved verbatim: 「前後関係がわかるように物体の後ろにあるものは半透明とかにしてもらったほうが良いかも\n角度によって青に見えている部分が赤に見える気がするが判断しかねる」. This is an observation about visible depth order and color legibility, not a change to or approval of classification, geometry, routing, Print Profile, validation or printability. Fixed camera-independent presentation now uses a blue circle for inside, orange triangle for outside and red cross for unresolved, each with a dark outline. Front markers are opaque; markers hidden behind BODY use the same hue with a non-blended 3/16 (18.75%) screen-door pass. The app switches between front-only and semi-transparent-back modes. If unresolved is zero, no red support-marker batch is created. The thin white support-free base footprint remains; mixed purple remains diagnostic-only and default OFF. Case A/B top/side/back browser evidence is stored as screenshots/skin-v088-depth-case-{a,b}-{top,side,back}-20260825.png with unchanged counts: A mixed208 / inside23281 / outside1261 / unresolved0 / duplicate0; B mixed40 / inside31250 / outside105 / unresolved0 / duplicate0. Rotation changes only front/back visibility, never classification color. No archive, high-resolution generation, Slice, print-candidate seal, tag, push, publish or deploy; printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "v088 footprint routing simplification: author observation and decision preserved verbatim: 「複雑なので画像では判断できなくて実際のプレビューを見ている\nベースのかたちの内側部分も赤くなっている\nベースのかたち内側は一律で青くなるようにすればいいと思う」. This is a routing-rule decision based on the actual local preview, not approval of the whole classification or printability. One XY outer footprint is built only from the support-free host balls, host blend and source-to-mm scale; Dry Web, scaffold, patch holes, generated/fused BODY and diagnosis Surface triangles cannot affect it. Every valid site inside or on the boundary routes blue to Dry Web; only sites outside route orange to removable scaffold. Mixed faces are diagnostic-only and purple is default OFF; invalid coordinates or unavailable footprint remain red and validation remains fail closed, including any red site inside the footprint. CLI, Worker and app share the same footprint and routing. Actual browser Surface48/119.5mm/45deg results: Case A mixed208 / inside23281 / outside1261 / unresolved0 / duplicate0 / footprint68 vertices; Case B mixed40 / inside31250 / outside105 / unresolved0 / duplicate0 / footprint69 vertices. White footprint plus blue/orange default overlays are preserved in two new top-view screenshots, while the existing top/side/back review switch remains available. No archive, high-resolution generation, Slice, print-candidate seal, tag, push, publish or deploy; printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "v088 exception review checkpoint: author observation preserved verbatim: 「現状は問題なさそう\n単純な球体に穴が一つの形状になってしまったので例外があるかがわからない」. This refers only to the simple sphere-plus-one-hole baseline and is not approval of the whole classification, absence of exceptions, or printability. Without changing shape or classification logic, the local app now switches between Case A (byte-exact actual v087 Shape Recipe, SHA-256 e510cd475ba8c9fc071afd817d566a09ef3c8a3cc2ea6a8e51f7b32a1cdb661f) and Case B (existing yohaku-skin-plate-20260719 boundary-stress recipe, SHA-256 c4c7e836b8acc3789e8aa341c01e249bc7a2da4ec358f2613a771f2682c06b46), with top/side/back views at Surface48, 119.5 mm and 45 degrees. App Worker counts: A mixed1139 / inside8636 / outside15906 / unresolved0 / duplicate0; B mixed1180 / inside13165 / outside18190 / unresolved0 / duplicate0. Six screenshots preserve inside blue, outside orange and mixed purple overlays; unresolved red remains zero. Author judgment is pending; no archive, high-resolution generation, Slice, print-candidate seal, publication, deploy, or printApproval change; printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "SKIN v088 Phase A review checkpoint: author observation preserved verbatim: 「消えすぎていて印刷すると絶対失敗する。オーバーハング部はベース形状の外側はサポート、内側はDrywebでの補強が必要」. Each overhang face now contributes four independently classified support sites; mixed faces are diagnostic-only, inside sites route only to Dry Web, outside sites route only to removable scaffold, unresolved/duplicate/unassigned sites remain fail closed, and deterministic near-site dedup keeps one stable site. CLI, Worker and app share the same routing and validation. The app can overlay inside blue, outside orange, unresolved red and mixed-face purple outlines with an ON/OFF control and all five counts. The exact paired low fixture binds recipe SHA-256 93bdafd7f20ef34e239900739fd9633c3fda63a7931c814f80b2c77f877e0c8f / Seed phase-a-low / Surface24 / fused32 and records mixed173, inside2705, outside4483, unresolved0, duplicate0. The production principle 「更新されたアプリを操作した結果の形状を印刷候補にする」 is retained. Local browser review state is open at Surface24 with Profile match and the same mixed173 / inside2705 / outside4483 / unresolved0 / duplicate0 counts; screenshot screenshots/skin-v088-support-sites-review-20260824.png records the visible toggle and overlay. Author visual judgment is pending; no archive, high-resolution generation, Slice, tag, push, publish, deploy, or printer operation; printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "Print Profile v1 shared foundation: added katachi.skin.print-profile.v1 validation/canonical SHA and one low-resolution fixture; CLI and browser Worker now derive the same ResolvedPrintPlan, bind recipe SHA/Seed and recorded geometry/Dry Web/scaffold/printer conditions, and emit common validation facts. The app compiles with Profile JSON load/save and current mm/match display. Low-resolution CLI/Worker plan and validation-facts parity tests, related SKIN tests, test typecheck, production build, and git diff --check passed. Browser operation, high-resolution generation/comparison, Slice, physical reproducibility confirmation, deployment, and v087 artifact overwrite were not performed; printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "v087 base-interior-free print candidate: Author observation preserved verbatim: retraction0でプリントしている今のところ大丈夫そう / プリントに時間がかかるので次の案を作りたい / いまベースのかたち内部にもサポートが入ってしまっている / ベースのかたちは実体は無いがここはサポートではなく、Drywebで解決するところだと思うのでサポートの除去と必要であればDrywebの修正して. The sealed v086 artifact and public stable v0.65 remain unchanged. An optional host-interior policy now rejects 1,074 normal candidate faces inside the implicit base, 114 explicit targets inside the base, and 106 explicit targets whose vertical rail crosses BODY, while retaining 135 exterior explicit rails. Planned pillars fall from v086 585 to v087 413. The unchanged targetedGrid Dry Web remains 1,490 nodes / 823 edges and passes the internal A1 mini gate, so no evidence-driven Dry Web geometry change was made. Exact Surface128/fused240 output removes 72 closed detached scaffold components / 35,712 triangles only under a narrow scaffold-cross-section, largest-body-size, and <=2% fail-closed rule; final 3MF is 2,372,868 faces, watertight, degenerate-free, one component, plate spread 0, minimum first-layer footprint 2.085 mm >= 1.5 mm. SHA-256 a8db96f1836e8e55d29639a2549e4d74b0f6ecffefab41438ae8b489dfe7b5d4. Bambu Studio 02.08.02.61 Slice was deliberately deferred to preserve the active unsaved retraction-0 print project, so normal/strict G-code reachability and physical behavior remain pending; printApproval=false. No deployment was performed."
    },
    {
      "date": "2026-08-24",
      "note": "Screenshot observation: Bambu Studio 02.06.00.51 showed Generic PLA @BBL A1M > Setting Overrides > Retraction with only Length enabled at a measured baseline of 0.8 mm; all other retraction overrides were unchecked/N/A. The first untested printer-side comparison is Length 0.6 mm only, saved as a separate user preset, with no simultaneous Travel distance threshold, speed, temperature, or geometry change. This is a 25% one-step hypothesis, not a proven anti-clog value. No setting was changed by the observer, no reslice/print was started, and the sealed v086 artifact remains unchanged with printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "Screenshot-grounded Q&A preserved verbatim: どこだ. On the supplied Bambu Studio 02.06.00.51 screen, the correct entry is the circular ellipsis at the right edge of the Generic PLA row under Project Filaments, not the currently open Process > Others > G-code output > Reduce infill retraction control. No UI action, setting change, reslice, geometry change, or deployment was performed."
    },
    {
      "date": "2026-08-24",
      "note": "Q&A preserved verbatim: bambustudioでどこを触ればいい？ For Bambu Studio 02.06.00.51, recorded the non-geometry path Prepare > Filament preset edit > Advanced > Setting Overrides > Retraction > Length. The current value must be recorded and changed by one small step in a duplicated user preset; Minimum travel is a separate frequency comparison and must not be changed simultaneously. No setting was actually changed, no reslice or print was initiated, and exact v086 remains sealed with printApproval=false."
    },
    {
      "date": "2026-08-24",
      "note": "Printer-side countermeasure question recorded verbatim: データを直すのではなく例えば3Dプリンターで引き戻しを減らすのはどうだろう？ Retraction reduction was kept as an untested hypothesis, not a confirmed fix. The note distinguishes reducing retraction length from reducing retraction frequency through minimum-travel behavior, records stringing/ooze/restart-blob tradeoffs, and limits the next comparison to three printer-side candidates tested one variable at a time. Exact v086 geometry and sealed artifacts remain unchanged with printApproval=false. No setting, code, mesh, 3MF, calculation, publication, or deployment was changed."
    },
    {
      "date": "2026-08-24",
      "note": "Physical print observation recorded verbatim and classified as a print-blocking issue: データではなく3Dプリンターの問題で小さな点（今回はサポート）をたくさん打つと詰まってしまうので対策を考える必要がある. The author currently identifies the problem as printer-side rather than malformed shape data. Cause candidates and human judgment points were kept separate; no cause was declared. Three non-implemented next comparisons were recorded: isolate nozzle/feed/material conditions, correlate the clog with dense short-extrusion/retraction layers in slicer Preview, and only if printer-side measures are insufficient compare a shape-preserving continuous support path. Exact v086 remains sealed with printApproval=false. No code, recalculation, artifact generation, publication, or deployment was performed."
    },
    {
      "date": "2026-08-24",
      "note": "v086 PRINT-CANDIDATE SEAL: Existing 3MF, recipe, normal/strict reachability reports, validation JSON and SHA-256 list were preserved without recalculation. Current SKIN-scoped changes were checkpointed under local tag skin-v086-print-candidate-checkpoint-20260824; unrelated result.json was excluded. status=AWAITING_PHYSICAL_PRINT_OBSERVATION and printApproval=false. Public stable v0.65 was not changed, no publication/deployment occurred, and no next candidate was generated."
    },
    {
      "date": "2026-08-24",
      "note": "v086 physical-print feedback candidate: Author observation preserved verbatim: 印刷をしてみた / まず物体がおもったより小さかったので1.5倍位にしたい / サポートは今の細さを維持してDrywebは強度が必要なので1.5倍の太さにする. The exact v071 recipe remains unchanged at internalRadius=0.045; target longest dimension increased from 80 to 119.5 mm (1.49375x), which makes Dry Web physically 1.49375x thicker without double-scaling the parameter. Removable scaffold shaft remains 1.40 mm diameter; only the bed foot is 2.40 mm diameter. Exact v086: bbox 115.835 x 85.604 x 119.353 mm, 3,983,584 faces, one watertight component, 585 pillars, Bambu A1 mini PLA 0.20 mm 597 layers, automatic support off. Normal and strict raster reachability both have 0 floating components. Bambu Floating vertical shell diagnostic markers remain, therefore status=AWAITING_PHYSICAL_PRINT_OBSERVATION and printApproval=false. 3MF SHA-256 a1328a1171b396f641e5be3864c13776b30ff4b0c913bafdcb53a8de3ed48e87. Public stable v0.65 is unchanged and no deployment was performed; v077-v085 remain undistributed intermediate artifacts."
    },
    {
      "date": "2026-08-23",
      "note": "PRE-PRINT SEAL: status=AWAITING_PHYSICAL_PRINT_OBSERVATION and printApproval=false. Public stable app v0.65 is frozen and unchanged (Cloudflare Version ID eb6ff21f-2541-440d-9232-dfb53bc30c1a); no deploy was performed. The separate print-artifact series promotes exact v071 only: both normal and strict raster reachability pass all 400 layers with floating layers/components 0/0, while 14 Bambu Floating vertical shell markers across 12 layers remain a known discrepancy. Print artifacts v057-v070 are superseded/rejected and v072-v076 are not promoted or distributed. Only SKIN and directly required common files are sealed; no new method, optimization, refactor, deletion, public-version change or deployment was performed. Next decision waits for one real print and observation."
    },
    {
      "date": "2026-08-23",
      "note": "v071 physical-test checkpoint: Author explicitly stopped additional implementation and deferred the new Support Blocker approach until after one real print. Exact v071 is frozen as a physical-test candidate. Normal and strict raster toolpath reachability both pass all 400 layers with floating layers/components 0/0, while Bambu Studio G-code still contains 14 Floating vertical shell markers across 12 layers; this discrepancy is preserved rather than reported as warning-free approval. Exact 3MF, recipe, normal/strict reachability reports, Bambu marker report and an integrated checkpoint JSON are saved in Downloads. v072-v076 experiments are not promoted or distributed. Next decision follows observation of the real print."
    },
    {
      "date": "2026-08-23",
      "note": "v0.69.0: The author question is preserved verbatim: 足元の調整でなんでこんなに時間がかかるの. Foot planning is now separated from final meshing with --plan-only: 44–51 s planning, then one final 8-worker fused160 remesh (271.87 s), Bambu slice 22.12 s and 400-layer reachability analysis about 6 s. Slice-feedback dedupe now keeps the highest nearby Z rail. Exact v071: Dry Web 1490/823 gate OK; reachability 11829/6894/4935; 222 cumulative feedback targets, 220 explicit rails, 380 total pillars; final Float32 mesh 1940176 faces / 969442 vertices, closed, winding-consistent, degenerate-free, one component; plate spread 0 and minimum first-layer footprint 1.5605 mm >= 1.50 mm. Bambu automatic support is off. Bambu retains its conservative floating-regions geometry warning, but actual 400-layer G-code passes both normal raster reachability and strict zero-offset/zero-area-exclusion reachability with floating layers/components 0/0. Automated geometry and toolpath gates are OK; exact v071 isolated Bambu layer 1→2→3 visual confirmation remains required before send-to-printer approval. The strict analyzer can now optionally emit layer 1–3 SVG evidence from the exact masks used for its verdict; v071 evidence shows 239 distributed first-layer islands and 179/165 layer-2/3 islands with unsupported count 0."
    },
    {
      "date": "2026-08-23",
      "note": "v0.67.0: Author words preserved verbatim: CPUはもっと使って良いんじゃない / solの設定変えたら早くなる？ / 他の処理も重いところは全部直して. Surface preview, exact inspection, normal export and fused 3MF now share exact Z-slice Worker meshing with up to 8 workers; inspection no longer freezes the UI or encodes unused STL, while bounded opening/gauge/partition paths keep their lower-memory shared-grid designs. The broad first-layer foot and removable tip now use independent junction radii: 1.8 mm foot, 1.4 mm shaft/contact, 1.0 mm foot height, 0.65 mm overlap. Exact author artifact at Surface128/fused160: Dry Web 1490/823 gate OK; reachability 11829/6894/4935; 168 pillars; final 1003512-face closed, winding-consistent, degenerate-free, one-component mesh; plate spread 0 mm, clearance -0.000000486 mm, minimum layer-1 footprint 1.5605 mm >= 1.50 mm. One-object/one-part 3MF is 15455899 bytes, SHA-256 38ccebf99f136ef76b5f715f1ba0bfa1c199f7198a67c39071f4536cbc1dd0d4, ZIP OK. Automated geometry approval=true; Bambu isolated layer 1→3 visual check remains required before printApproval. Final pass: the remaining synchronous Optimizer print-check meshing now runs in the dedicated 8-slice Worker, encodes STL without unused OBJ, reports slice/face/elapsed progress in the UI, cancels stale work on shape changes, and moves targeted Dry Web re-diagnosis plus motif-lowest-point extraction off the main thread. Surface diagnosis and staged preview are also allowed up to 8 workers."
    },
    {
      "date": "2026-08-23",
      "note": "v0.66.0: Author observations preserved verbatim: 増えたけど少ない / UIにも同じように処理状況が表示されるようにしてね / 今より早くできる方法はある？ v0.65 is rejected. The spherical foot was replaced by a 0.85 mm radius, 0.60 mm high fourth-power broad pad, with fail-closed checks for a >=1.50 mm layer-1 footprint (three 0.50 mm initial-line tracks), common plate plane and ±0.05 mm clearance. The 3MF Worker reports seven named stages, counts and elapsed time; identical repeat saves use an in-memory completed-archive fingerprint cache. Broad-pad bounds now extend the proven v0.65 contact-grid pitch/phase instead of resampling BODY. Tests 18 and TypeScript pass. Exact author runs still fail closed: resolution 128 gives 3 components and fused resolution 160 gives 2 components, both closed with degenerate/open/non-manifold all zero; no v0.66 3MF was emitted and printApproval remains false. Exact float32 component face-count/XYZ/Z-range diagnostics were added to CLI and UI Worker so the next expensive run identifies the detached geometry in one pass."
    },
    {
      "date": "2026-08-23",
      "note": "v0.65.0: The author inspected isolated Bambu layers and found v0.64 layer 1 ended at horizontal move 98 with only one start island, while layer 3 reached 4515; verbatim: 一部浮いてると思う / 98より右にはいかない. v0.64 is rejected and must not be printed. Root causes were a pre-rescale BODY plate reference and a discontinuous flat-bottom pillar SDF. Every pillar now starts 1.0 mm below the coarse BODY minimum with a radius-0.5 mm spherical foot, capsule shaft and spherical contact tip. Exact author run: 336 pillars, final 1100124-face closed/winding-consistent/degenerate-free one-component mesh, plate spread 0 mm, final plate clearance -0.000358 mm. Independent final-3MF slice at meshMin+0.10 mm has 190 components across X 73.28 mm / Y 53.52 mm. Archive 16713669 bytes, SHA-256 a032c24a843dcd13349ac23183cb8ea041cd2886c8e2576f3bcdde3e47fbe10c. Bambu isolated layer-1 visual confirmation remains required; printApproval=false. Published as Cloudflare Version ID eb6ff21f-2541-440d-9232-dfb53bc30c1a; public SKIN asset SHA-256 matches the local production build."
    },
    {
      "date": "2026-08-23",
      "note": "v0.64.0: The author confirmed that v0.63 still had floating regions (verbatim: さっきのところも直ってないな), proving that one 3MF part containing intersecting closed triangle components was insufficient; v0.63 is superseded and must not be printed. BODY, Dry Web and all 326 deterministic all-reachable columns are now sampled in one existing SKIN SDF mesh pass at fused resolution 160. The first 0.48 mm tip / 0.12 mm overlap attempt failed closed at 2 components; a 1.00 mm contact pad with 0.30 mm overlap produced a Float32-saved closed, winding-consistent, degenerate-free mesh with 0 open/non-manifold edges and exactly 1 connected component. Exact author run: graph 1490/823, gate OK, reachability 11829/6894/4935, 326 columns, final 1081140 faces, one 3MF object/component/normal part BODY_WITH_FUSED_SCAFFOLD, 16450865 bytes, SHA-256 e8717d22b8964f6b24e5b7d92c2e054c753af48172a118a6a4fcf74ba3d5e387. Published as Cloudflare Version ID 044289e7-6a68-4be8-bc88-33ec631c751c. The author Bambu re-slice removed the floating-regions warning; the supplied screenshot overlays layers 1 through 3, so isolated layer-1 versus layer-3 continuity still requires human confirmation and printApproval remains false. Subsequent isolated-layer inspection showed layer 1 ended at horizontal move 98 with one start island while layer 3 reached 4515; v0.64 is rejected and must not be printed."
    },
    {
      "date": "2026-08-23",
      "note": "v0.63.0: Actual Bambu Slice Preview of v0.62 still reported floating regions even after BODY and 83 scaffold columns were packaged as one normal mesh part. The author zoomed to the first 0.6 mm and observed many small overhang starts, so v0.62 is superseded and must not be printed. Dense dry scaffold keeps automatic Support disabled, reduces candidate spacing from 2.2 to 0.8 mm, uses a printable 0.80 mm shaft with a 0.48 mm breakaway tip and 0.12 mm BODY overlap, and increases the exact author-model scaffold from 83 to 326 columns. Exact candidate archive: 7291355 bytes, SHA-256 549e5b2ae430f73ad74033687d273b886b678cb9d768ca12a5881308c27256fd. Actual Bambu re-slice still reported floating regions; v0.63 is superseded by v0.64 and must not be printed."
    },
    {
      "date": "2026-08-23",
      "note": "v0.62.0: Actual Bambu Slice Preview of v0.61 still reported floating regions even after all 83 breakaway tips overlapped BODY, so v0.61 is superseded and must not be printed. The unchanged BODY and scaffold soups are now packaged as one BODY_WITH_SCAFFOLD normal mesh object/part instead of two component parts, keeping enable_support=0, 445048 total faces, the exact 83-column geometry, and all source statistics. Exact archive: 7120734 bytes, SHA-256 11b1615681448c7bc916b9cc9da1c6164ca08fbca33e9ea19ec1b0795c205383; human Bambu re-slice remains pending."
    },
    {
      "date": "2026-08-23",
      "note": "v0.61.0: Actual Bambu Slice Preview of v0.60 had automatic Support disabled and all 83 columns visible, but still warned that the object had floating regions because every column stopped 0.22 mm below BODY. v0.60 is superseded and must not be printed. Columns retain the exact same all-reachable 83-position selection, taper to a 0.48 mm breakaway tip, and overlap the target Surface by 0.12 mm so layer order is continuous while removal remains localized. Exact package: 7122938 bytes, SHA-256 1ea3606c91842ac0d55a3bc10c83f6bab612c47e446aeddc04f59a80b8832226; human Bambu re-slice remains pending."
    },
    {
      "date": "2026-08-23",
      "note": "v0.60.0: The author correctly rejected v0.59: 15 perimeter columns cannot support the full diagnosed overhang field. v0.59 is superseded and must not be printed. Default scaffold coverage now uses every plate-reachable candidate across the full XY field, rejects complete-BODY corridor collisions, reduces spacing from 3.2 to 2.2 mm, increases shaft diameter from 1.04 to 1.10 mm, and keeps Bambu automatic support disabled. Exact author run: graph 1490/823, gate OK, reachability 11829/6894/4935, all-reachable coverage 6894, BODY-collision 4768, short 280, spacing 1763, 83 closed scaffold components / 5312 faces / open0 / nonmanifold0, archive 7123010 bytes, SHA-256 e62a0e6d50508baf98aa61c4a14061bd04eda29711a04c27b88ac7a7fa1c94b0. Full tests, TypeScript and production build pass; Cloudflare Version ID 6ba00639-c9f5-478a-937a-64f823ab1aaf. Human Bambu Slice Preview remains pending."
    },
    {
      "date": "2026-08-23",
      "note": "v0.59.0: Actual Bambu Preview showed normal(manual)/Snug/build-plate-only still filled the porous interior (17.17 g support for 19.88 g model); v0.58 is superseded and must not be printed. Katachi now generates deterministic closed straight columns only within a 4 mm outer XY hull band, rejects columns whose nine-sample vertical corridor intersects the complete BODY, stops 0.22 mm below the target, packages BODY and EXTERNAL_SCAFFOLD as normal printable parts, and explicitly disables Bambu automatic support. Exact author run: graph 1490/823, gate OK, reachability 11829/6894/4935, 15 closed scaffold components (960 faces), archive 7073080 bytes, SHA-256 4433e712d0bb8b5963e752908d23cfa8db7e8d1f1475fc8ee1e47712241928d0; full tests, TypeScript and production build pass; Cloudflare Version ID 072d21b1-e76c-418b-aae1-4f185a73a53f. Human Bambu Slice Preview remains pending."
    },
    {
      "date": "2026-08-23",
      "note": "v0.58.0: Actual Bambu Slice Preview showed tree(manual) branches entering the porous SKIN interior despite external-only Enforcer contacts. Tree export is now fail-closed and removed from the UI. Bambu 3MF fixes normal(manual) + snug + support_on_build_plate_only=1 + expansion0. The earlier v0.57 tree artifact is superseded and must not be printed."
    },
    {
      "date": "2026-08-23",
      "note": "v0.57 exact author Surface run reached reachability and found 11 Float32 tiny but nonzero faces misclassified by a fixed area threshold. Support occlusion now accepts finite triangles unless their cross product is exactly zero; nonfinite or exact zero-area/collinear faces still fail closed, preventing silently skipped occluders."
    },
    {
      "date": "2026-08-23",
      "note": "v0.57 saved-STL winding correction: exact author BODY STL (439736 faces) was closed, degenerate0, one component but had 70 Float32 winding-inconsistent edges. Gate Worker now orients before gate/cache export and rechecks saved topology; CLI repairs winding-only supplied BODY without moving coordinates (70→0 verified), otherwise fails closed. Bambu GUI warning remains human-check pending."
    },
    {
      "date": "2026-08-23",
      "note": "v0.57 exact author CLI run succeeded at resolution128/target80 with required v1 --body-provenance supplied BODY reuse: wall time 7:41.42 on this machine (not a print-speed claim); BODY winding 70→0, graph 1490/823, gate OK, reachability candidate/kept/rejected/invalid 11829/6894/4935/0, archive 8456624 bytes; 3MF SHA-256 3ddb52ddaf4c209f45af63a41e339bbcf66188447baceb7fabb0037282691296. Independent 3MF BODY XML topology: 439736 faces; degenerate/open/nonmanifold/winding 0; components1; Enforcer 55152 faces; actual package topology OK. Headless Bambu CLI --info exits 139 for both new and previously GUI-opened old 3MF, so GUI human check remains pending. Reusable --body-stl now requires a strict v1 --body-provenance binding raw recipe/STL SHA-256, target/resolution/internal mode, and replayed graph counts before package output. Support Enforcer uses the same finite/exact-zero predicate as reachability; raw prism and indexed package faces must each equal keptFaceCount*8 or export fails closed."
    },
    {
      "date": "2026-08-22",
      "note": "v0.57.0: Bambu Support Enforcer candidates now pass a deterministic, fail-closed four-sample straight-down final-Surface lower-envelope filter. Actual bounded matrix (3 host seeds × random PACK / quadFlow regular / varied / field-curvature / Voronoi-CVT / Goldberg × Dry Web / Voronoi Edge, resolution 30) completed 36/36 invariant and deterministic rerun checks; evidence is notes/support-reachability-matrix-20260822.json. This is not final-resolution print proof."
    },
    {
      "date": "2026-08-22",
      "note": "Research observation only: Bambu Studio manual Support Enforcer is recognized, but its tree paths naturally enter the porous SKIN interior around the Dry Web because the current candidate filter knows residual risky faces but not exterior/build-plate reachability or removal access. The next design step is an exterior, downward-envelope, bed-reachable filter; a blanket internal Support Blocker is deferred because it may also erase support needed by the outer shell. No geometry, 3MF, UI, or export behavior changed."
    },
    {
      "date": "2026-08-22",
      "note": "v0.56.1: Corrected the first real Bambu slice after the author observed BODY warnings and 40.07 g of support for a 19.88 g model. The v0.56.0 exporter incorrectly converted all 57,562 pre-Internal red faces (460,496 enforcer faces), duplicating regions already mitigated by the accepted Dry Web. Internal-enabled 3MF now converts only afterDangerPositions, the exact final-mesh red faces still unsupported after fusion; Internal-none keeps beforeDangerPositions. Added a regression test for this selection. The observed 18 MB package itself passed ZIP validation and Bambu Studio 02.06.00.51 CLI parsing with return code 0 / Success, so the orange icon is a non-fatal Bambu warning, but the original over-supported 3MF must not be printed."
    },
    {
      "date": "2026-08-22",
      "note": "v0.56.0: Added a Phase 1 Bambu Studio 3MF export for outer-SKIN support designation. Exact final-resolution red diagnosis faces become closed thin triangular-prism Support Enforcer geometry that overlaps BODY; BODY and enforcer are stored as volumes of one logical object with manual tree or normal support enabled. The exporter reuses the exact final Surface triangle buffer, or the exact matching A1 mini Internal-gate OK STL when Internal geometry exists, and runs XML/ZIP generation in a Worker without rebuilding the field. Compatibility is pinned to and CLI-parsed by installed Bambu Studio 02.06.00.51 using its production-extension object/part subtype model. Bambu sliced Preview remains the human gate; Support Blocker and per-triangle paint-on supports are intentionally deferred."
    },
    {
      "date": "2026-08-22",
      "note": "Recorded the author's print-surface question: the current STL path has no geometric smoothing/remesh stage; the flat-normal staged preview exaggerates visible triangle edges, while actual faceting can only be reduced in-app by raising final resolution up to 224. At 80 mm, resolution 160/192/224 corresponds to 0.50/0.42/0.36 mm grid spacing, with field sampling cost growing approximately cubically. Blender Shade Smooth is display-only; any applied geometric smoothing invalidates the existing gate fingerprint and must be rechecked."
    },
    {
      "date": "2026-08-22",
      "note": "v0.55.2: Public Cloudflare pages now identify the local Optimizer boundary before starting a request and immediately direct the author to http://localhost:5174/skin.html instead of waiting and reporting an unresponsive engine. The public UI shows the same local-workbench link beside print preparation; localhost keeps the one-screen cached-STL Optimizer flow unchanged. No public origin was granted access to the Mac-local diagnostic service."
    },
    {
      "date": "2026-08-22",
      "note": "v0.55.1: Removed repeated exact-mesh work from the print path. The A1 mini Internal gate now returns and retains the exact STL it judged, immediately reuses an unchanged prior gate result, and can consume an already-built exact staged-mesh triangle buffer for the same history/graph/resolution/80 mm fingerprint. Gate progress now reports stage and elapsed time and may use up to eight slice workers. Integrated Optimizer checking requires a matching OK gate for Internal geometry, sends that exact cached STL instead of synchronously rebuilding it on the UI thread, uses the documented quick estimate, and reports connection/upload/poll progress with bounded retries. Geometry, gate thresholds, recipe format, and the approved Dry Web are unchanged."
    },
    {
      "date": "2026-08-22",
      "note": "v0.55.0: Replaced the gate-failing selected-target XYZ lattice with a deterministic Surface-rooted Dry Web while retaining the existing targetedGrid recipe key for replay compatibility. Every exact final-mesh motif-lowest marker receives an inward material-directed contact, and all motifs are joined by a minimum spanning web over their actual sphere-envelope gaps; the count control now adds redundant short ties instead of dropping motifs. The fixed 5 mm bridge gate now measures only the consecutive edge interval outside the existing Surface material, not centre-to-centre length embedded inside both motifs. Default final resolution is 128 so the current 1.69 mm strut is represented by 2.7 voxels. On the editable dense-flower v6-style fixture at 80 mm, exact local browser measurement passed: watertight, one final part, zero saved-coordinate degenerates, 1490 Surface-fused nodes, zero unsupported nodes/edges, 314 bridges, zero over 5 mm, maximum exposed bridge 1.4 mm. Ordinary Internal-enabled 3D export is consequently enabled for that unchanged fingerprint. Outer-SKIN support remains delegated to Bambu Studio and real-machine success is not guaranteed."
    },
    {
      "date": "2026-08-22",
      "note": "v0.54.0: Added a fail-closed Internal-only print gate for the author's Bambu Lab A1 mini / 0.4 mm nozzle / PLA / 0.2 mm layer setup. Outer-SKIN support remains explicitly delegated to Bambu Studio. The gate builds the same final-resolution fused mesh, then requires watertight single-component topology, no saved-coordinate degenerates, at least 0.80 mm strut diameter, at least 2.5 voxels across that diameter, Surface-fused graph roots, bottom-up self-support within 45 degrees of vertical, and bridges no longer than the conservative project limit of 5 mm. These margins are Katachi criteria, not official Bambu guarantees. Internal-enabled ordinary mesh export is blocked until the current shape/options pass; mutations invalidate the result. The current v6-style fixture honestly fails with 150 final-mesh components, 61 unsupported nodes, 70 unsupported edges, and 30 overlong bridges."
    },
    {
      "date": "2026-08-22",
      "note": "v0.53.0: Implemented the author's requested red-point-driven Internal Structure as Targeted XYZ Grid. The exact final-mesh motif-lowest markers are deterministically thinned to a requested support count, inset by one shared radius, and connected through host-contained axis-aligned paths to a dry XYZ lattice; unreachable paths are omitted and counted. The same existing Internal graph preview/mesh/export pipeline is reused. The exact Surface mesh is built once, then before/after reach is recalculated on those same buffers. This remains a geometric contact screen, not a slicer support, strength, or print-success claim."
    },
    {
      "date": "2026-08-22",
      "note": "Research Q&A: recorded the author's question about generating Internal Structure toward the red final-mesh lowest-point markers. The working hypothesis is target-driven dry support: cluster nearby red targets, descend short Z struts, and snap them to a shared deterministic XYZ Grid/rail instead of building one organic branch per marker. Keep count/spacing and radius as the primary controls, then recompute the exact final mesh diagnosis after union. Documentation only; no geometry or UI behavior changed."
    },
    {
      "date": "2026-08-22",
      "note": "v0.52.1: Corrected the diagnostic evidence after the author rejected a coarse/bead view as meaningless. Face angles and per-motif lowest points now wait for the exact author-selected final Surface mesh resolution, generated through the shared parallel slice path. Lowest points are actual final-mesh vertices attributed to motifs by the smallest local sphere field; smooth-overlap ownership remains a disclosed heuristic. The former resolution-36 mitigation result is retained only as superseded research evidence, never as the current diagnostic."
    },
    {
      "date": "2026-08-22",
      "note": "v0.52.0: Added an optional per-motif lowest-point diagnostic. Each flower, coin, flat ring, or 3D ring gets one marker at the minimum realized sphere envelope z-radius after relational bridge/surface-connector points are excluded. Red means the Internal graph does not directly overlap the sphere that owns that low point; teal means it does. This is a source-sphere contact proxy, not a slicer island, support, or print-success proof."
    },
    {
      "date": "2026-08-22",
      "note": "v0.51.0: Added a simple, threshold-controlled outer-face angle diagnosis with explicit +Z build direction and 0-degree vertical / 90-degree downward-ceiling convention. Before mode colors every over-threshold triangle red; after mode separates triangles reached by the current Internal edge graph into teal while leaving unreached danger red. The finite-resolution edge-contact heuristic runs in a Worker and intentionally makes no slicer, bridge, thermal, strength, or print-success claim."
    },
    {
      "date": "2026-08-22",
      "note": "Design direction only: recorded the author's requirement that Internal Structure remain a dry contrast to the organic outer SKIN and work primarily through strut count and thickness. XYZ Grid is the next deterministic control structure; Voronoi Edge remains the irregular comparison. No geometry, recipe, UI, or export behavior changed in this revisit."
    },
    {
      "date": "2026-08-22",
      "note": "v0.50.0: Added a display-only Internal observation control with Normal, translucent SKIN, and hidden SKIN modes. The independent Voronoi graph remains opaque while Surface mesh/beads/labels/selection aids can be faded or removed; raymarch switches to the uncapped bead view when needed. Generation, history, inspection, partitioning, and mesh export remain unchanged."
    },
    {
      "date": "2026-08-22",
      "note": "v0.49.0: Restored the complete preserved v0.48 SKIN authoring UI and its supporting behavior after the deployed page lost Undo, the floating result viewer, staged mesh/Ghost modes, dense-flower v6 tools, lace fill, contact checks, element editing, opening maps, saved-reference viewing, partition/export controls, and related workflows. Added Voronoi Edge as an independent Internal Structure layer with Density/Radius/Randomness, shared preview and export meshing, deterministic recipe replay, and focused regression coverage; Surface generation remains unchanged."
    },
    {
      "date": "2026-08-12",
      "note": "v0.48.0: Coin bulge keeps the existing amount and adds a continuous back-only/equal/front-only balance, mirrored across CPU field, GLSL preview, workers, opening measurement, partitions, export provenance and recipe replay. Selected isolated motifs can be duplicated with an offset and can move individually between surface/center/inside through replayable edits; legacy recipes retain equal-sided bulge and surface placement. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-12",
      "note": "v0.47.0: Coin motifs gain a central-opening parameter from the exact historical solid path at 0 to an adaptively connected minimum-width outer rim at 0.95; the value is available in global generation, rotating 3D preview, selected-element reshape, recipe replay and Undo. A geometry-neutral Normal/Ghost viewport style adds translucent double-sided low-shadow mesh/bead viewing for reading the far side; raymarch explicitly returns to Normal because its first-hit field cannot reveal hidden surfaces. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-12",
      "note": "v0.46.0: Surface motif IDs are quiet by default and appear only for the pointer-hovered or selected motif; the optional representative always-visible mode remains available. Opening ID/area/perimeter cards are assigned to non-overlapping viewport-perimeter slots outside the projected form and persistent viewport controls, with depth-tested anchors, leader lines, and manual drag override retained. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.45.0: Individual ring3d editing now has a direct centerline-diameter value independent from tube thickness, with live 3D preview, realized history replay, and Undo. Opening measurement defaults to a shape-height-aware automatic cross-section so raised flowers/ring3d are not misread at their tangent feet; manual signed offset remains available. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.43.0: Opening-map measurement now starts on the actual host surface (0 mm) rather than at a 2 mm outward offset. A positive offset that moves beyond the motif relief and merges almost the entire sampled host surface into one uncovered component is explicitly flagged as an offset-merged diagnostic instead of being presented as one ordinary opening. The requested offset and finite-resolution measurements remain unchanged and auditable. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.42.0: Selected-motif position dragging now updates only that motif's lightweight source beads once per animation frame and records the exact final edit only on release; raymarch receives a temporary wire proxy while bead/mesh modes reuse their existing instance path. Viewport-local individual controls stop pointer propagation, so the selected motif is no longer deselected before its update click; dirty and applied point-count feedback make the result explicit. Exact STL/OBJ construction and encoding moved to a cancellable Worker with elapsed-time status while the viewport remains interactive. The current working model is now the primary opening-map action, while the older saved v6 reference is explicitly labeled read-only and separate. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.41.0: Separated the host-relative placement of the primary surface generation from the later largest-gap lace-fill pass. The author can keep initial motifs on the surface while centering only gap-fill motifs across it or moving them fully inside. The new choice is replayable and legacy recipes default to the historical surface placement. The selected-motif quick scale/rotate toolbar now keeps a larger cursor-safe zone so direct position dragging can begin unobstructed. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.40.0: Newly generated and explicitly reshaped ring3d motifs retain their authored primary nodes and record only the additional interleaved spheres needed to make every cyclic edge, including the closing edge, overlap. Connectors follow the ring axis/frame by angular, radial, and axial interpolation, retain the primary radial envelope, and are stored in the realized history result; replay intentionally leaves legacy point arrays unchanged. ringNodeCount continues to mean authored primary nodes while the UI's existing realized-point count exposes the total. This changes ring generation semantics only; screen mesh, export/inspection, linking calculation, packing rules, and printability claims remain distinct. Measured verification facts and topology limits are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.39.1: Selecting a flower, coin, or ring in generated-mesh view now overlays only that motif's original source beads as a depth-tested pale wireframe. The established 1.2x bead-view selection outline remains unchanged; mesh view uses a near-source 1.025x envelope solely to avoid surface z-fighting. A selected editable motif also gets a viewport-local Japanese quick toolbar for 10% shrink/enlarge and 15-degree rotation; its projected anchor follows orbit/zoom, hides when unavailable, and routes every button to the existing replayable edit path. The individual motif reshape button now receives the same selection eligibility, stays disabled for connection-owned motifs, and rechecks it before recording. Selection clearing, A/B visibility filters, view switching, and dense opening-map mode hide the wire and toolbar through the same selection state without rebuilding the full bead set. Live mortar/coverage/components/linking/overlap gauges keep their existing calculations but run in a generation-guarded, burst-coalesced Worker; screen mesh, export, and print inspection remain separate and make no printability claim. Measured verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.39: Fixed the disconnected individual-edit workflow: a real 3D patch click now synchronizes the compact registry/editor selection. Selected isolated motifs move by arrow keys (Shift = 4x) or a direct surface-tangent drag, and every completed move records the same replayable editPatch operation for multi-step Undo. Dense picking uses one direct point-sphere pass before the expensive composite raymarch. Preview meshing now compiles realized points into contiguous numeric buffers and divides its exact global Z grid across up to six nested Workers; slice concatenation is regression-tested against the single-pass triangle order, while audited inspection/export remain on their existing path. The upper-left view controls and upper-right Undo dock no longer overlap at the measured desktop viewport. Surface element IDs now fade from front to back while the selected label remains opaque. Measured verification facts and limitations are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.38.1: Whole-motif contact reinforcement no longer silently leaves geometry unchanged when no neighbour is reachable inside the selected cap. It first performs the minimal contact-solving pass, then enlarges every still-weak motif to the explicit whole-scale cap, remeasures contacts, and keeps unresolved IDs visible. The UI cap now accepts 0..1 (100%..200%), explains that enlargement can remain unresolved, and tells the author when to raise the cap. Verified against the supplied 123-flower/3,075-point recipe without modifying the input file."
    },
    {
      "date": "2026-08-11",
      "note": "v0.38: Weak-contact reinforcement now offers the historical closest-point/petal growth or uniform whole-motif enlargement about the flower/coin/ring centroid; the selected mode, cap, and exact realized point scale provenance replay through recipes. Screen mesh display is progressive: it first installs the density-capped responsive mesh, then keeps that mesh interactive while a second Worker restores the exact author-selected resolution. The final 32..224 resolution is directly editable beside the viewport modes; while conversion runs, a non-blocking bottom status bar reports the real stage and elapsed time and offers cancellation. Dense mesh sampling also compiles invariant patch-shape groups once instead of filtering and allocating those groups at every voxel; the compiled evaluator is numerically checked against the point-query formula. This improves authoring fidelity and avoids main-thread stalls, but does not claim Blender-equivalent speed or printability. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.37: Reorganized persistent authoring chrome around the viewport: view modes stay at upper left from page load, multi-step Undo history stays at upper right, and selected-element position/shape properties stay at lower left without covering the right authoring panel. Step 4 now uses a drag/wheel 3D motif preview plus a separate random-PACK flower connection diagram driven by fusion amount and separate/fused mode. Newly generated motifs can preserve the historical surface placement, center their complete envelope across the host surface, or sit fully inside it. Screen mesh display now builds capped low-resolution transferable position/normal buffers in a cancellable Worker while the current view remains responsive; high-resolution inspection/export is unchanged. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.36: Added selected-motif shape editing. New generated motifs retain their own shape-value snapshot; selecting an isolated flower, coin, flat ring, or 3D ring opens a compact local preview and the saved values (or clearly labeled current-setting fallback for legacy recipes). Applying records the exact regenerated patch as reshapePatch without repacking neighbours, preserves identity/review records, clears stale analyses/partitions, and Undo replays the prior realized geometry. Relational bridge/surface-connector patches are refused. Step 4's family-wide preview is kept separate and its flex sizing no longer collapses."
    },
    {
      "date": "2026-08-11",
      "note": "v0.35: Added a compact single-motif live preview at the start of step 4. Coin irregularity/bulge, flat-ring hole/node/wobble, 3D-ring tube/node/wobble, and flower petal/core/opening/neck/cupping/lift/growth controls update it immediately. It is explicitly a legibility preview, not the generated curved-surface geometry, connection result, mesh, or print claim. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.34: The initial and post-Undo zero-patch viewport now renders the full smooth host volume as an authoring preview instead of an empty plate-mode field. The instruction chip moved below the form so it no longer obscures the base silhouette. This changes only the live raymarch preview; CPU compositeSdf, mesh inspection, STL export, recipes, and print claims are unchanged. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.33: Added an always-visible sticky undo control at the upper right of S-skin plus Windows Ctrl+Z / macOS Cmd+Z. Undo replays the exact recorded history after removing one entry, keeps the initial visible host baseline, and therefore takes the first surface packing back to the host-only state without re-running packing randomness. Imported-recipe provenance is cleared after an edit by undo because the current history no longer equals the imported bytes. Added focused baseline/first-pack undo coverage; the complete surface suite, test typecheck, and production build passed. Browser and deployed-site verification are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.32: Added the presentation-only 立体レース workflow profile and a selected-element editor. Each accepted local size/rotation/tangent-position edit records the full realized Patch plus intent in editPatch history, preserving patch-set revision and review annotations while clearing stale partitions and analysis. The transform clones inputs, preserves off-surface normal lift, scales every explicit realized radius only for size edits, and refuses flower bridge networks or surface-connector owners rather than regenerating connections. Automated surface tests, test typecheck, and production build passed. Browser verification at 127.0.0.1:5174/skin.html confirmed v0.32, the current-theme card and original five navigation buttons, 12 host balls / 51 default coin patches, and S・コイン 001 size/rotate/move edits advancing history 2→3→4→5 with no console warnings or errors."
    },
    {
      "date": "2026-08-11",
      "note": "v0.31: Added compact generated-element search and review records. Surface annotations are keyed by patch-set revision plus Patch ID, so a fresh pack cannot inherit a note from a reused numeric ID; exact records replay through exported history."
    },
    {
      "date": "2026-08-11",
      "note": "v0.30: Added the shared generated-element naming vocabulary used by both S-skin and S-interior-growth. Surface motifs are labeled S・花/コイン/平リング/立体リング plus their persisted Patch ID. A default-on small overlay shows a deterministic representative sample of at most 24 elements and always includes the selected patch; the toggle can remove the overlay without changing geometry or history."
    },
    {
      "date": "2026-08-11",
      "note": "v0.29: Added a realized-sphere contact proxy that counts distinct touching neighbour motifs and colors the uncapped bead view by weak/near-target/target-reached contact count. Added an optional deterministic local reinforcement pass: only the closest point pair of motifs below the requested neighbour target can grow, each point has an explicit absolute cap, unreachable motifs remain reported, inputs are cloned, and the exact adjusted result including separate contactR provenance is recorded through packPatches history before N partition. This is explicitly not FEA, final-mesh connectivity, slicer support, or print-strength evidence. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.28: Preserved the admired dense-flower v6 result as an explicitly read-only STL reference instead of falsely presenting it as a replayable Skin recipe. Its independent offset-2mm/resolution-112 opening report and top-40 opening meshes can now be opened directly in S-skin, with the supplied six-view atlas, categorical surfaces, area/perimeter rows, draggable three-line DOM tags, and leader lines. Added a generalized multi-stage lace-fill post-process for every primary surface organization and every existing motif type: deterministic largest-gap passes reduce the allowable size band while retaining an authored clearance, preserving recognizable motifs, mixed sizes, and lace openings rather than globally swelling the field. New realized patches and the lace controls are recorded through the existing exact-result packPatches recipe contract. Positive clearance is explicitly not a structural-connection guarantee. Verification facts are recorded in README Observation."
    },
    {
      "date": "2026-08-11",
      "note": "v0.27: Added an off-main-thread 空隙マップ for current S-skin fields, including random PACK, QUAD-FLOW, Voronoi/CVT, and Goldberg realized patches. The Worker first uses the exact current buildSkinMesh contract and its returned scale, then samples an outward host-gradient offset surface to estimate uncovered connected regions, finite-resolution area/perimeter/shape index, and categorical identity overlays. IDs are assigned before the display 10/20/40/all filter, so changing that filter does not recompute or renumber. Worker cancellation, generation/request guards, and mutation invalidation clear stale labels, colors, and values. The result is explicitly a finite-resolution derived-surface estimate, never an exact boolean curve, safety, or printability claim. Automated test/type/build verification is recorded in README; no deploy was performed per the task constraint."
    },
    {
      "date": "2026-08-10",
      "note": "v0.25: Fixed resolution-sensitive fragmentation in old QUAD recipes without modifying their saved histories. A new live mesh-join width is stored on newly generated local connection points, while mesh and N-partition builds non-destructively supply the missing difference only to legacy QUAD points that already carried fusion provenance. On the author's supplied 107-entry, 150-flat-ring varied-QUAD recipe, the unchanged input first reconstructed as watertight but fragmented (92 components at resolution 48 and 57 at 64); with the default 0.12 mesh join it measured 60,236 / 108,752 faces, watertight, exactly one component, and 455 repaired legacy points at resolutions 48 / 64. Added the first selectable Voronoi/CVT prototype: deterministic Fibonacci seeds, two approximate spherical Lloyd relaxation passes by default, outer projection to the implicit host, mutual six-neighbour local graph plus minimum spanning completion, one existing motif per seed, and the same optional local connection controls. It is explicitly labeled a prototype because it does not yet construct exact polygon boundaries, a geodesic/anisotropic CVT, curvature weighting, or dedicated fitting pieces. Real-browser verification generated 48 flat rings on the default host: 48 seeds, 136 graph edges, 0 projection failures, 0 open graph edges; its resolution-48 mesh measured 65,952 faces, watertight, and one component. The strong local connection visibly narrows or can close ring holes, so this is not yet a final ring-surface solution. Automated S-skin suites passed 248 logical checks, test-source type checking and the 18-page production build passed. No slicer, support, strength, adhesive, magnet, or print-success claim was added. Kept local so the published v0.19 checkpoint remains preserved."
    },
    {
      "date": "2026-08-10",
      "note": "v0.24: Reorganized the single-page authoring UI into an explicit five-step flow: 1 base form, 2 surface organization, 3 fill shape, 4 adjustment/generation, and 5 form-following partition. A compact five-button index scrolls to each real section. Surface organization is now one six-card comparison instead of separate mode and tiling toggles: random PACK, regular quad, and varied quad are selectable; curvature-aligned quad, Voronoi/CVT, and hexagon-plus-pentagon stay visible but disabled and labeled research candidates rather than being implied as implemented. Coin, flat ring, 3D ring, and flower are likewise shown as four descriptive cards. QUAD shared-edge connection moved after shape selection into adjustment; shape/manufacturing details and result gauges/manual tools are collapsed disclosures, while the generate action and result line remain visible. Existing history, generation, mesh, and N-partition behavior is unchanged. Real-browser verification followed the new cards from varied quad to 3D ring to generation, produced 96 patches with 314 adjusted spheres, maximum addition 0.103, 0/192 open edges, 0 projection failures, then used the five-step index to reach partition. Automated partition/motif suites remained at 228 logical checks; test-source type checking and the 18-page production build passed. No mesh, slicer, strength, joining, or print-success claim was added. Kept local so the published v0.19 checkpoint remains preserved."
    },
    {
      "date": "2026-08-10",
      "note": "v0.23: Generalized QUAD-FLOW shared-edge connection from flowers to coin, flat ring, 3D ring, and flower through the common realized sphere-set representation. A shape-neutral local mode inspects every cell edge, finds the closest sphere pair, and enlarges only those components enough to contact plus an edge-relative overlap; a separate mode preserves authored radii. The UI now exposes this common choice and warns that ring holes can narrow locally. Added deterministic nonuniform all-quad tiling: three seed-derived symmetric cube-axis interval sets vary cell sizes while preserving the same closed 6*n*n topology, paired boundaries, and Euler-required special cells. QUAD replacement now resets its cell-ordered patch IDs so identical settings and seed reproduce identical realized motifs without an external reset. Primary-source research records curvature/field-aligned quads, anisotropic surface CVT/Voronoi hex-dominant tilings, Goldberg hex/pentagon topology, and print-aware decomposition; Voronoi/hex paths remain labeled unimplemented. In the real browser on the default 12-ball host at n=4 nonuniform mode, coin adjusted 258 spheres (max 0.143), flat ring 274 (max 0.152), and 3D ring 314 (max 0.103); every case reported 0/192 open shared edges and one adjacency group. The nonuniform runs kept 96 cells, 24 fitting candidates, 8 extraordinary vertices, and 0 projection failures, and a same-settings 3D-ring repeat was byte-for-byte equal as a realized result. Automated partition/motif suites passed 228 logical checks including 61 QUAD-FLOW checks; test-source type checking and the 18-page production build passed. Full mesh, ring-hole clearance, slicer support, strength, joining, and print success are unverified. Kept local so the published v0.19 checkpoint remains preserved."
    },
    {
      "date": "2026-08-10",
      "note": "v0.22: Replaced QUAD-FLOW's one common all-component flower expansion with local gap repair. For each shared quad edge, the solver finds the closest sphere pair between its two flowers and grows only those two components enough to close the measured clearance; the existing flowerExpansion value now controls additional edge-relative overlap after contact, not a global swelling multiplier in QUAD-FLOW. Every realized patch stores quadCellId provenance, and the result reports adjusted sphere count, maximum local addition, inspected edge count, and still-open edge count. Random PACK keeps its existing whole-flower PACK-SPIKE expansion unchanged. The author's supplied 40-entry recipe (12-ball host, n=4, ten petals, no core, expansion 0.25) was imported through the real file input and repacked: 310 of 2,880 spheres changed, maximum local addition was 0.104, and open shared edges were 0/192; estimated coverage was 27.7% and the live adjacency estimate was one group. Automated QUAD-FLOW coverage includes the supplied restrained ten-petal setting and verifies local rather than global adjustment. A full real mesh for this heavier supplied host was not completed in this pass; watertightness, slicer behavior, support, strength, and print success remain unverified. Kept local so the published v0.19 checkpoint remains preserved."
    },
    {
      "date": "2026-08-10",
      "note": "v0.21: Changed QUAD-FLOW motifs from centre-sized placeholders to full-cell forms. Every authored point is converted back into the cell frame, its complete centre-plus-radius footprint is scaled to the quad domain with 4% boundary overreach, bilinearly mapped through the four corners, and reprojected to the host surface; this applies to the selected existing coin, ring, or flower definition without adding a second shape generator. In fused flower mode, the expansion reference is now derived from all pairs of cells sharing a quad edge rather than only a flower minimum-spanning tree. At expansion 1, one common addition makes all 192 shared-edge pairs of the n=4 closed grid overlap by a small edge-relative margin, using only the motifs' own swollen surfaces and no bridge geometry. The separate mode remains intentionally separable, expansion below 1 may still separate, and stronger values deepen the overlap. Real browser verification used a one-ball radius-1.5 host, n=4, four-petal flowers, and expansion 1: 96 flowers used common width 0.075, estimated coverage 64.5%, one adjacency group, and no deep-overlap warning. The resolution-64 mesh measured 172,136 faces, 80.0 x 80.0 x 79.9 mm, watertight and one component, with no console errors or warnings. Automated suites passed 189 partition/motif assertions including 22 QUAD-FLOW assertions, 19 Study-catalog assertions, and 18 shared PACK-SPIKE assertions; TypeScript and the 18-page production build passed. This remains a cube-guided projected grid rather than a curvature-direction remesher; special fitting geometry remains future work, and slicer/support/strength/print success remain unverified. Kept local at this checkpoint so the previously published v0.19 remains preserved."
    },
    {
      "date": "2026-08-10",
      "note": "v0.20: Added QUAD-FLOW as an additive experimental surface principle while preserving randomPack as the default and leaving every v0.19 history replayable. A closed cube-sphere topology supplies 6*n*n four-corner cells, projects shared vertices to the outer implicit host surface, and reports rather than hides Euler-required extraordinary vertices. Cyan grid edges are ordinary cells; orange cells touch a non-valence-4 vertex and are explicit future fitting candidates, but receive the same placeholder motif for now so the first experiment has no artificial holes. The selected existing patch shape is authored once per quad; flower parameters and optional direct surface fusion are reused unchanged. surfaceGenerationMode, quadDivisions, and complete realized patches persist in the recipe. On a real one-ball radius-1.5 browser run with n=4, the UI reported 96 cells, 24 fitting candidates, 8 extraordinary vertices, and 0 projection failures. One four-petal flower per cell produced 96 motifs / 1,632 points; direct fusion used common width 0.099, estimated coverage 73.4%, and one adjacency group. The resolution-64 mesh measured 156,508 faces, 79.7 x 80.0 x 79.7 mm, watertight and one component, with no console errors or warnings. Automated suites passed 185 partition/motif assertions, 19 Study-catalog assertions, and 18 shared PACK-SPIKE assertions; TypeScript and the 18-page production build passed. This is a cube-guided projection prototype, not a production curvature-direction quad remesher; concave/non-star-shaped hosts may report projection failures. Dedicated fitting geometry and true four-corner bilinear motif deformation remain intentionally unimplemented."
    },
    {
      "date": "2026-08-10",
      "note": "v0.19: Replaced flower presets as the only authoring choice with explicit PACK-SPIKE-compatible flower controls. The primary controls are petal count 3..12, core on/off, and a 0..2 common expansion multiplier where 1.0 is the automatically derived connected reference, 0 keeps original component radii, and values above 1 deepen direct flower-surface fusion. An optional Flower Details disclosure exposes opening, neck, core size, cupping, core lift, and deterministic growth difference. The four former presets remain as shortcuts that write the same parameters; any manual edit becomes Custom. A new 'この設定で花を詰め直す' action replaces only flower patches while preserving coin/ring patches. Each realized point now retains fusionBaseR separately from fusionR, preventing repeated slider changes or manual flower edits from compounding expansion. History stores every custom value and the complete realized geometry; old preset histories remain replayable. Real in-app-browser verification used a one-ball radius-1.5 host with 8 petals, no core, expansion 1.25, and cupping 0.45: 64 flowers used common fusion width 0.191, estimated coverage 80.6%, and one adjacency group; the resolution-64 mesh measured 175,160 faces, 80.0 x 80.0 x 79.5 mm, watertight and one component, with no console errors or warnings. Automated partition suites report 41+50+11+9+11+45 = 167 passed; Study catalog 19 and shared flower-packing 18 tests passed; TypeScript and the 18-page production build passed. Slicer behavior, support, strength, adhesive, magnets, and printer success remain unverified."
    },
    {
      "date": "2026-08-10",
      "note": "v0.18: Corrected the meaning of flower integration after the author supplied the PACK-SPIKE printable-lace-shell reference image. Integration now means densely placed flower surfaces swollen into one continuous shell, not flowers joined by visible MST branches. The UI primary choice is now '一体の花（融合）'; the old direct/MST value remains replayable only for already-recorded v0.17 histories and is no longer offered. Packing first places every flower at its unexpanded size, then derives the smallest common component expansion from the widest closest-surface edge in the flowers' minimum spanning tree and expands every component uniformly. This matches PACK-SPIKE's fusion-radius principle while adding no line, branch, connector object, or hidden host shell. Realized baseR/fusionR values are stored on motif points, so replay is deterministic and repeated Pack/mode changes are idempotent. Manual flower add/delete recomputes the common expansion from the remaining current flower set and stores the realized result. Mesh inspection is invalidated on every host/patch mutation, preventing an earlier shape's topology report from remaining beside a new result. Real in-app-browser evidence: on the default 12-ball host, 69 flowers used common fusion radius 0.163, estimated coverage 58.7%, one adjacency group, and a resolution-64 mesh of 131,456 faces that was watertight and one component. On a one-ball radius-1.5 host matching the reference silhouette, 77 flowers used fusion radius 0.178, estimated coverage 68.5%, and a resolution-64 mesh of 186,684 faces, 78.0 x 80.0 x 79.6 mm, watertight and one component. A resolution-48 three-way curved partition grouped 16/36/25 flowers and completed in 20.1 s; parts measured 24,540/46,116/36,904 faces, each watertight and one component, with 0.26% original-versus-parts volume difference. N real-mesh overlap/gap testing remains unimplemented, so the parts remain VERIFICATION. Automated partition suites report 41+50+11+9+11+35 = 157 passed; Study catalog 19 and shared flower-packing 18 tests passed. No slicer, support, structural-strength, adhesive, magnet, or printer-success guarantee is made."
    },
    {
      "date": "2026-08-10",
      "note": "v0.17: Added a direct flower-network mode to PACK-SPIKE / MOTIF ON SURFACE. Instead of hiding a continuous host shell under the packed motifs, the realized flower patches are joined by a deterministic minimum spanning tree: N flowers create N-1 shortest candidate links, each link follows sampled points near the host surface, and overlapping bridge spheres become explicit PatchPoint geometry with role=bridge. Existing recipes remain separate by default; the chosen flowerConnectionMode and the complete realized motif/bridge geometry are stored in history, so replay never reruns the network construction. Manual flower add/delete in direct mode rebuilds the same deterministic network, and diagnostics ignore only these intentional bridge overlaps. The primary mesh and the N=2..6 path use this direct flower composite when bridge geometry exists; ordinary plate shapes retain the established continuous-shell N path. N source selection is derived from realized role=bridge geometry rather than the next-Pack UI toggle, so ordinary and partition meshes cannot disagree after toggling. Host grow/field changes/S1 imports invalidate confirmed partitions at history level, preventing an old result from being saved with new provenance. Real in-app-browser verification selected the 6-petal core motif, packed 69 flowers with 68 links and 294 bridge points (2,019 realized points total), and estimated one connected component. At resolution 48 the generated mesh was watertight but still measured 15 components, proving the thin connections are resolution-sensitive. At resolution 64 it measured 51,284 faces, watertight, and exactly one connected component. A three-way curved partition grouped the flowers 11/24/34 and completed in 108.3 s: parts measured 8,972/17,280/25,108 faces, each watertight and one connected component, with 0.08% original-versus-parts volume difference. N real-mesh overlap/gap testing remains unimplemented, so these outputs stay VERIFICATION. Automated partition suites report 41+50+11+9+11+24 = 146 passed; Study catalog tests report 19 passed; the shared flower-packing suite reports 7 passed; test TypeScript checking and the 18-page production build passed. Limitations are explicit: the MST optimizes Euclidean proximity rather than visual composition or true geodesic distance, bridge paths are sampled-sphere heuristics that may cross concave regions, coarse mesh resolution can disconnect thin links, and no slicer, support, adhesive, magnet, structural-strength, or printer success is guaranteed. Not deployed because the experimental direct-network path and unrelated dirty workspace changes require author review before public publication."
    },
    {
      "date": "2026-08-10",
      "note": "v0.16: Integrated PACK-SPIKE / MOTIF ON SURFACE into S-skin as a fourth packed shape, so the author can choose, pack, preview, partition, verify, and save without moving between Studies. The adapter reuses PACK-SPIKE's four motif preset definitions and flower component generator, adds short sampled neck spheres matching its component connections, normalizes the complete motif into S-skin's existing clearance envelope, rotates it in each host anchor's tangent frame, and lifts it so its lowest sphere touches the arbitrary host surface. The packed operation stores the realized PatchPoint geometry, so history replay does not rerun motif generation. Flower geometry is treated as raised raw geometry rather than clipped into a flat plate; the N-part path unions it with the already-established continuous host shell and assigns whole flower instances through the same deterministic Patch adjacency ownership. UI shape-specific controls now hide coin/ring-only knobs and show only four PACK-SPIKE motif presets for the flower choice. Real in-app-browser verification selected 10 petals without a core, packed 56 flowers / 1,680 realized points with 444 rejected attempts, proposed three groups of 25/14/17 flowers, and built resolution-48 verification parts in 35.7 s. The three outputs measured 27,832 / 27,072 / 20,140 faces, each watertight and one connected component; original-versus-parts volume difference was 2.27%. Browser console had no errors. Automated suites report 41+50+11+9+11+10 = 132 passed; test TypeScript check and the 18-page production build passed. Current limitations are explicit: the arbitrary host mapping uses a local tangent-frame approximation rather than geodesic deformation, necks are sampled spheres rather than PACK-SPIKE's exact capsule field, N real-mesh overlap/gap testing is not generalized, and no slicer, printer, support, adhesive, or magnet behavior is guaranteed. Not deployed because this experimental path and unrelated dirty workspace changes require author review before public publication."
    },
    {
      "date": "2026-08-10",
      "note": "v0.15: Added generation-native N=2..6 partitioning directly to S-skin. The author selects N, the existing packed Patch adjacency/geometry proposes deterministic N ownership groups, the same page previews those groups in up to six colors, and a Worker generates curved ownership boundaries without a planar cutter. In plate mode, ordinary S-skin plates are intentionally disconnected, so the N-part path explicitly treats Surface Packing as the boundary/detail design and adds a continuous host shell underneath; window mode keeps its existing continuous composite. Every generated STL is separate from the source and accompanied by the replay recipe, SHA-256 output hashes, group/seed selection, measured saved-Float32 topology, connected-component count, volume difference, and limitations. N output remains labeled VERIFICATION because the A/B real-mesh Monte Carlo overlap/gap gate is not yet generalized. Real-browser default-pack verification produced 51 patches grouped 20/19/12. The pre-continuous-shell build was honestly rejected as 14/13/8 disconnected components; after the correction, a resolution-48 build completed in 10.3 s with all three parts watertight and one connected component each, while the measured original-versus-parts volume difference was 2.44%, so the UI reported 要確認 rather than printability. The blank plate-mode viewport at zero patches now explains that the author must press 詰める instead of appearing silently black. Automated partition suites report 41+50+11+9+11 = 122 passed; test TypeScript check and production build passed. No printer/material calibration or printability/adhesive-strength guarantee is asserted. Not deployed; author review is still pending."
    },
    {
      "date": "2026-08-10",
      "note": "v0.14: Katachi S-skin and Optimizer diagnostics are connected in one screen. The current generated field is meshed and encoded as STL in memory, posted directly to the local Optimizer engine, and the measured topology, dimensions, estimated wall p05, estimated internal-overhang ratio, and best-orientation candidate are returned to the same panel. No intermediate STL download or Optimizer WebUI navigation is required. Katachi still owns recipe/field/patch adjacency; Optimizer still owns post-mesh diagnostics. The launchers start only the Optimizer calculation engine alongside Katachi. Verified through the real browser buttons with the default 51-patch pack: the same page reported a closed 33-component mesh, 76.3 x 71.4 x 80.0 mm, estimated wall p05 2.74 mm, estimated internal-overhang 0.0%, and -Y as the first orientation candidate at estimated internal-overhang 0.0%. These are geometry estimates, not printer/material calibration or a printability guarantee. Existing A/B partition behavior is unchanged; N-way generation-native grouping is not part of this integration round."
    },
    {
      "date": "2026-07-20",
      "note": "Coin-bulge generation-side experiment (v0.13, docs/sonnet-instruction-20260720-katachi-coin-bulge-study.md): author observation verbatim -- 'CoinSRFで言うとコイン部分が平べったくなっているから一つ一つのコインがふっくらとして結果サポートが不要な形になっていると自然になりそうだなと考えている'. This is a HYPOTHESIS under test, not a settled conclusion -- UI/README/history/reports say only 'コインのふくらみ候補' / '内部サポート危険域の推定比較', never 'サポート不要'/'印刷可能'/'安全'. A/B proposal logic, gate thresholds, tutorial, and coin center/radius/placement/Patch-ID/adjacency-graph generation are all unchanged. New SkinParams.coinBulge (default 0, UI range 0-0.32, step 0.005, label 'コインのふくらみ') widens ONLY the coin-shape shell band in plate mode from thickness/2 to thickness/2+coinBulge, letting a coin's own round point geometry push outward on both sides of the host surface up to its raw (unclipped) extent; flatRing/ring3d/window mode are entirely unaffected. coinBulge<=0 runs the EXACT pre-existing code path (coin+flatRing unioned into one field, then intersected with the shell once) rather than 'split with a zero-width extra band', because smooth booleans (opSmoothIntersection) are not distributive -- intersection(shell, union(coin,flatRing)) is not guaranteed to numerically equal union(intersection(shell,coin), intersection(shell,flatRing)) even at zero extra width, so a literal separate branch was required to guarantee old-recipe replay produces byte-identical results. Implemented identically in CPU (field.ts's compositeSdf, new hostBandSdf helper shared with shellSdf) and GLSL (shaders.ts): extended the uPatchData[i].y fractional shape encoding from a single ring3d bit to three shapes (coin +0.00, flatRing +0.25, ring3d +0.50), decoded via isCoinPoint/isFlatRingPoint/isRingPoint, no new uniform arrays (only one new scalar uCoinBulge). Enumerated every compositeSdf call site via `rg -n \"compositeSdf\\(\"` and threaded coinBulge through all of them explicitly (never an implicit default): picking.ts (raymarchComposite/estimateCompositeNormal), meshExport.ts (buildSkinMesh), partition.ts (buildPartitionMeshes, 6 internal call sites), renderer.ts's update() uniform write, and every main.ts call site. coinBulge is a live field-shape parameter read by compositeSdf for every EXISTING patch (unlike minR/maxR/irregularity/gap/attempts/patchShape/ring-specific knobs, which only affect patches created by a future 詰める) -- extended this round's invalidation to thickness/roundK too (instruction §3.2 explicitly permits this 'if the common-function route is safer/minimal'): changing any of the three now invalidates a built partition result/Worker/export/metrics via the existing invalidateStalePartitionResult() (new thin wrapper invalidateStaleResultForShapeParamChange overrides the status text to '形状設定が変わったため、同じA/B構成でも物理分割をもう一度実行してください' instead of the draft-edit wording) WITHOUT touching state.partition or the confirmed A/B grouping itself, so no re-confirm is ever required, only a rebuild -- verified: build to completion, change coinBulge, hasResult flips false, state.partition's groupA/B unchanged, draftMatchesConfirmed() stays true, real step stays at 7 (no regression to 6), and a fresh rebuild succeeds immediately. PartitionBuildRequest gained an explicit coinBulge field (never implicitly defaulted inside the Worker); partition provenance gained shapeParameters:{thickness,roundK,coinBulge}; makeSkinExportBaseName embeds coinBulge into the filename when >0 (e.g. yohaku-skin-plate-coin-bulge-0p080-20260720), unchanged at 0. Recipe formatVersion stays 1 (DEFAULT_SKIN_PARAMS.coinBulge=0, handled generically by the existing setSkinParam operation, no old-recipe compatibility work needed). UI: コインのふくらみ slider integrated into the existing skin-slider mechanism, plus static (non-recommending) preset buttons 0/+0.04/+0.08/+0.12 and a short one-line state ('0: 従来' / '>0: ふくらみ比較中'); beads-mode caption gets an explicit warning when coinBulge>0 that beads draw raw PatchPoint spheres and cannot show the shell-clipping difference, so beads must never be mistaken for a bulge preview; changing the value auto-discards any stale 全体メッシュ overlay (pre-existing afterMutation() path, unmodified) and never auto-regenerates the heavy overlay on slider drag (only an explicit 全体メッシュ selection triggers a rebuild). Verified end to end with real coordinate clicks (window.__skin used solely to read state -- getPatches/getSkinParams/getViewMode/getPartitionResult/getPartition -- never to drive an action): lightweight fixture (28-coin pack, minR/maxR widened for a coarser packing) showed identical Patch-ID/shape/point fingerprints across coinBulge=0/+0.08/+0.12 (only lateral positions matter for this check, confirmed unchanged) while the raymarch view visibly went from flat/edge-on discs to progressively rounder bumps at the same camera pose; flatRing-only and ring3d-only fixtures were pixel-identical between coinBulge=0 and +0.12; window mode was pixel-identical between 0 and +0.12 for a coin fixture. Real CoinSRF (141 coins, ID 392-532, all shape=coin, no coinBulge operation in the recipe itself so it loads at 0) compared via 全体メッシュ resolution-48 previews (beads intentionally NOT used for this comparison, per instruction §4's explicit prohibition on treating beads as a bulge preview): coinBulge=0 rendered as the flat, crinkled, overlapping-plate look matching the author's '平べったい' observation (2.75s synchronous build); +0.08 showed individually rounded, clearly puffy coin bumps with visible-but-not-collapsed valleys between neighbors (2.85s); +0.12 was more pronounced still, individual coins still distinguishable, overall CoinSRF silhouette preserved (2.84s). Honestly recorded that buildSkinMesh's 全体メッシュ path is NOT Worker-ized (unlike partition builds) and blocks the main thread synchronously for the full ~2.8s on every rebuild -- an existing limitation, not something this round introduced or fixed. No author was present in this session to select a candidate value, so per instruction §7 item 7 ('作者が候補をまだ選んでいない場合は、STLを4種類量産せず、ブラウザ比較画面で止める') this round stopped at the browser comparison screen -- §8's Optimizer diagnosis comparison (value 0 vs a chosen candidate) was NOT performed. Explicitly unconfirmed and documented as such: real slicer support-generation behavior (never tested), whether the visible valleys between bulged coins become new external support-risk regions (untested), and full-resolution (96) STL topology/watertightness at any coinBulge value (only the coarse resolution-48 preview was built). Added new src/studies/skin/coinBulge.test.ts (11 assertions: coinBulge=0 matches both the coin-only and the coin+flatRing COMBINED pre-split formula exactly, not a split-then-union; a probe point between the old shell edge and the new coin band goes solid only when bulged; bulge never extends solid material past the coin's own raw field even at bulge=5.0; flatRing-only/ring3d-only/window-mode fields are byte-identical across bulge values; computeSkinSamplingBounds already covers the coin's raw radius so a bulged mesh can never be clipped; old-recipe replay leaves coinBulge at the default 0; export/import round-trips a non-zero value exactly; buildPartitionMeshes produces different part volumes for different coinBulge, proving the value actually reaches the Worker's own builder and isn't silently dropped) plus wired it into package.json's test:partition and tsconfig.test.json's include (Worker-lifecycle interaction with a coinBulge change mid-build -- cancellation and no-late-result-adoption -- is a browser-only concern per the pre-existing documented Vite-Worker-under-Node limitation, not re-tested here beyond what v0.11's existing mechanism already covers structurally). Partition tests 102 (41+50+11), test-source typecheck, eight-page npm run build, git diff --check all passed. Partition math, A/B proposal, gate thresholds, tutorial, and Patch generation logic are all unchanged this round. Not committed; not deployed (author review pending)."
    },
    {
      "date": "2026-07-20",
      "note": "Selection-visibility final polish (same-day amendment to v0.12, docs/next-agent-instruction-20260720-katachi-selection-final-polish.md; version left at 0.12.0, not bumped): the 1.2x pale-yellow wireframe outline, non-selected dimming, A/B fill-color preservation, A/B endpoint badges, the always-visible selected-patch label + assign-button disabling, and the tutorial short-line/collapsed-details were all kept as-is per instruction -- fixed only two leftover UI-state inconsistencies from the audit. P0: onToggleSeedPickMode(false) ('両端選択を中止') previously only called updateOperationFocus(), which cleared the viewport frame/chip but left seedAId and its A badge behind if only the A endpoint had been picked -- the author couldn't visually tell 'cancelled' from 'A confirmed'. Added a shared discardEndpointSelection() (clears seedAId/seedBId/seedPatchIds/draft A+B, invalidates any stale result/Worker via the existing safe path, then refreshPartitionDraft() to redraw badges/colors/tutorial/focus from the same cleared state) and now call it from BOTH onToggleSeedPickMode(false)-while-incomplete and onClearSeeds (previously duplicated inline), consolidating responsibility. Completed A+B pairs never reach the false branch -- handleClick's seed-pick branch flips seedPickMode itself the instant both are set -- so this never touches the existing 'badges persist through propose' behavior. Verified with real clicks: picked A (badge appears, switches to B-wait), pressed 'cancel', and confirmed via the read-only debug handle that getSeedIds() returned [] (seedAId/seedBId actually null, not just hidden), viewport.className emptied, the A badge disappeared, and the panel read 'A端 未選択 / B端 未選択' again. P1: computeViewportFocus() previously showed the A/B chip/frame/row-emphasis for ANY patch selection whenever patches existed and add-patch-mode was off -- even during ordinary Pack/delete/mesh-inspect work with no A/B draft, confirmed partition, or open guide, making A/B assignment look like the primary action when the author wasn't doing A/B work at all. Added a new pure derivePartitionViewportFocus(input) to partitionTutorial.ts taking an explicit inPartitionContext boolean (so the function itself never touches the UI layer, avoiding circular refresh calls) computed in main.ts from seedPickMode || (draftGroupA.size+draftGroupB.size>0) || state.partition!==null || (tutorial open && real step === 5). Also had to split the row's .action-emphasis CSS class out of setPartitionSelectedPatch (which still drives the label text and assign-button disabled state off selection alone -- assigning to A/B is still how a fresh draft gets started, so buttons stay usable outside 'context') into a new setPartitionActionEmphasis(active) called with active = focus==='selected' -- found and fixed a real bug during verification where, without this split, the emphasis border kept lighting up on ANY plain selection regardless of context, contradicting the very requirement being implemented. Verified with real clicks: right after Pack (no A/B context) the chip stayed hidden even after selecting a patch (outline/label/button-enable still worked, confirming those three are correctly NOT gated); starting endpoint selection still showed the blue/orange frames+chips+badges exactly as before; after proposing (draft now exists, real A/B context), the chip/frame/emphasis correctly reappeared and cycled between unselected and selected states; Aのみ/Bのみ filter and OrbitControls rotation showed no regression from v0.12. Added 7 new partitionTutorial.test.ts assertions for derivePartitionViewportFocus covering the instruction's 6 documented cases plus a no-patches edge case. Partition tests 91 (41+50), test-source typecheck, eight-page npm run build, git diff --check all passed. Not committed; not deployed (author review pending)."
    },
    {
      "date": "2026-07-20",
      "note": "Selection visibility during A/B assignment (v0.12, docs/next-agent-instruction-20260720-katachi-selection-visibility.md): author observation verbatim -- '選択してA/Bに変更するときに選択しているものの表示を変えないと選択できているのかわからない' / '文字はあまり読まないので文字で説明は重要ではなく、今すべき操作を強調表示が大事' / '選択はかなり強調しないと今のままではわからない。A端B端を選ぶときも同じ'. Partition math, proposal, Worker, gate, and history unchanged. Root cause: renderer.ts's recolorBeads() unconditionally preferred A/B/unassigned group color over any selection color whenever a T13 grouping was active (i.e. for the entire A/B workflow) -- selecting a patch never changed its bead color at all, exactly matching the author's report. The old SELECTED_COLOR was also too close to GROUP_B_COLOR (orange-ish) to double as a recolor even if it had been wired up. Fix keeps A/B/unassigned color on the selected bead UNCONDITIONALLY (never replaced) and instead layers two independent, non-color cues: a separate small InstancedMesh ('highlightMesh') drawing an oversized (1.2x) bright pale-yellow wireframe shell over just the selected patch's own bead instances, rebuilt cheaply from existing instance matrices on every selection change (no full patchBeadMesh rebuild); and dimming every OTHER bead to 50% brightness while a selection is active (both within the instruction's 45-60%/1.15-1.25x ranges, no blink/pulse). Hidden automatically when the selected patch's own group is filtered out via Aのみ/Bのみ. Added a permanent one-line 選択中Patch: #ID / 現在 A（青） display (aria-live=polite, color swatch + text) directly above the Aへ/Bへ buttons via new pure functions resolvePartitionSelectionGroup (draft-set membership, with an explicit non-silent 'conflict' state if a patch were ever in both sets -- defensive, unreachable via the existing assign paths which always delete-then-add) and describePartitionSelectionLabel in partitionTutorial.ts; the assign buttons are now disabled while nothing is selected. Added a viewport-level 'what to do next' system (main.ts's computeViewportFocus/updateOperationFocus): a static frame (box-shadow, no layout shift) plus a floating chip cycle through 待ち (① coinをクリック, faint white frame) -> selected (② AかBを押す, frame cleared, a static non-animated .action-emphasis border+tint on the whole Aへ/Bへ row instead, both buttons shown at equal visual weight since software doesn't pick A or B) -- suppressed while in manual-add-patch mode. Extended the same visual grammar to A/B endpoint picking: starting 'A端・B端を選び直す' shows a thick blue frame + 'A端をクリック' chip; picking A switches instantly to an orange frame + 'B端をクリック' chip and adds a camera-facing canvas-texture Sprite badge (ring + big 'A' letter) pinned to the endpoint patch's own point; picking B adds the matching orange 'B' badge and ends seed-pick mode (frame/chip clear automatically since focus falls through to the manual-selection state); both badges persist through 'propose' since they're driven by seedAId/seedBId, not by pick-mode itself; '両端選択を中止' clears the frame/chip immediately (verified: viewport.className empties on the very next tick). Real bug found and fixed during implementation: the badge Sprites were built with depthTest:true and positioned at the seed patch's OWN anchor point -- i.e. dead center of an opaque bead sphere -- so they were completely invisible behind the sphere's own front face (confirmed via screenshot: zero badges visible despite correct seedAId/seedBId state). Fixed by setting depthTest:false/depthWrite:false/renderOrder:10 on the badge material so it always draws on top; verified after the fix that both A and B badges render clearly as large ringed letters floating over their coins. Tutorial text shortened per author's stated preference for minimal reading: added a `short` one-line imperative field to TutorialStepContent (e.g. Step 2: 'A端をクリック → 次に反対側のB端をクリック') shown as the card's primary always-visible content; the old multi-bullet body moved into a <details><summary>詳しく見る</summary> collapsed by default and re-collapsed on every step change. Step 5's advance ('確認した') button gained data-tutorial-target='confirm-review' added to that step's highlightTargets, so after an A/B fix the existing tutorial-highlight mechanism (unchanged otherwise) emphasizes the next real action -- only while the guide is open and the real workflow is actually at step 5, never forcing auto-confirm and never highlighting outside the guide. Verified end to end against real CoinSRF (141 coins) using only real coordinate clicks (window.__skin used solely to read state -- getSelectedPatchId/getSeedIds/getDraftGroups/getViewMode -- never to drive an action): selecting an unassigned patch showed the bright outline + '#448 / 現在 未割当', assigning to A recolored it blue while keeping the outline and updating the label to '#448 / 現在 A（青）' with the row's yellow emphasis border appearing; selecting a different (B) patch showed its own outline while #448 stayed blue at full brightness (both A/B colors visible simultaneously, only the currently-selected one highlighted); a B→A reassignment (patch #507, real click through propose -> select -> confirm label read '#507 / 現在 B（オレンジ）' -> real click 'Aへ' -> label became '#507 / 現在 A（青）', outline never dropped); re-clicking the selected patch cleared selectedPatchId, the outline, and reset the label to 'なし', re-disabling both assign buttons; switching to 'Aのみ' while a B patch was selected hid that patch AND its outline together (no orphaned outline left floating, confirmed by screenshot); orbiting the camera (real click-drag) kept both the manual-selection outline and the A/B endpoint badges correctly pinned to their target patches' new screen positions (structurally guaranteed since both are real Object3Ds in the scene graph, not screen-space overlays, but verified visually anyway). Added 10 new partitionTutorial.test.ts assertions: 4 for resolvePartitionSelectionGroup (A/B/unassigned/conflict), 5 for describePartitionSelectionLabel (all four states plus the conflict wording), 1 verifying every one of the 8 tutorial steps has a non-empty short line distinct from its title. Partition tests 84 (41+43), test-source typecheck, eight-page npm run build (skin bundle grew from ~77KB to ~83KB), git diff --check all passed. Not committed; not deployed (author review pending)."
    },
    {
      "date": "2026-07-20",
      "note": "Invalidation status-text honesty correction (same-day amendment to v0.11, docs/next-agent-instruction-20260720-katachi-invalidation-status-honesty.md; version left at 0.11.0, not bumped): partition math, Worker termination, and gate thresholds unchanged -- fixed two status-display inaccuracies left by the in-flight race fix above. Problem 1: invalidateStalePartitionResult() cleared a COMPLETED result's export/metrics (hadResult=true, hadRunningWorker=false path, e.g. editing the draft after a build finished) without ever calling setPartitionStatus(), so the old '完了 ...' text stayed on screen even though partitionResult/exports/metrics were already gone underneath it. Problem 2: the always-invalidate-unconditionally policy (kept as-is, safety over precision) had a status message that always said 're-confirm', even when the edit was a no-op relative to the last confirmed configuration (e.g. clicking 'assign to A' on a patch already in A) -- re-confirming in that case is genuinely unnecessary, only a rebuild is. Fixed by adding a pure describePartitionInvalidationStatus(hadRunningWorker, stillConfirmedAfterEdit) to partitionTutorial.ts and calling it unconditionally (not just when hadRunningWorker) from invalidateStalePartitionResult(), which now also evaluates draftMatchesConfirmedPartition(draft, state.partition) right after the edit to get stillConfirmedAfterEdit; hadResult/hadRunningWorker detection, Worker termination, and result/export/metrics clearing are otherwise unchanged. Subject wording distinguishes 'an in-flight build was killed' (実行中だった分割) from 'a completed result was discarded' (前回の分割結果); stillConfirmedAfterEdit distinguishes 'もう一度確定してください' (re-confirm needed) from '確定構成は変わっていないため、もう一度物理分割してください' (rebuild only). Verified against a lightweight fixture (default 12-ball host, 51 coin patches, endpoints #4/#22, 50/1 proposal) with real coordinate clicks only (window.__skin used solely to read state -- workerRunning/hasResult/draftMatchesConfirmed/selected patch id -- never to drive an action): (a) built to completion at resolution 32 (1.3s, gate-failing but genuinely COMPLETED), then a real click moved a different patch (#25) to the opposite group (breaking the confirmed match) -- the stale '完了 (経過1.3秒) ...' text was replaced (not left showing alongside the new null result), gate-metrics text disappeared, export buttons went disabled, and the real step fell back to 6; (b) re-confirmed, started a fresh build at resolution 224 (raised via the real 解像度 slider specifically to keep the Worker alive long enough to interact with), and while workerRunning:true, a real click selected patch #4 (already in group A) and clicked 'assign to A' (a no-op relative to the confirmed groups) -- workerRunning flipped to false immediately (Worker still safely stopped), draftMatchesConfirmed() stayed true, the real step stayed at 7, and the status text read '...確定構成は変わっていないため、もう一度物理分割してください' rather than asking to re-confirm; a 3-second re-check confirmed hasResult/workerRunning both stayed false (no late Worker message adopted, unchanged from v0.11). Added 4 new partitionTutorial.test.ts assertions for describePartitionInvalidationStatus's four (hadRunningWorker, stillConfirmedAfterEdit) combinations. Partition tests 74 (41+33), test-source typecheck, eight-page npm run build, git diff --check all passed. Not committed; not deployed (author review pending)."
    },
    {
      "date": "2026-07-20",
      "note": "In-flight Worker/draft race fix (v0.11, docs/next-agent-instruction-20260720-katachi-inflight-draft-fix.md): partition math, gate thresholds, and coin shapes unchanged -- this round fixed state concurrency and guide display only, not partition quality. P0: invalidateStalePartitionResult() previously started with `if (!partitionResult) return;`, which is trivially true for the ENTIRE duration of an in-flight build (partitionResult is only set on successful completion), so real draft changes made while a build was running silently no-op'd -- partitionGeneration never advanced, the running Worker kept computing against the old confirmed groups, and its late result could be adopted since the generation check also never fired. Fixed by checking `partitionResult !== null || activePartitionWorker !== null` (preserves the original no-op for the common 'nothing built yet' case) and, when either is true, unconditionally advancing partitionGeneration, terminating and nulling any active Worker, clearing setPartitionBuildRunning/partitionResult/export flags/metrics, updating the status text, and refreshing the tutorial. Chose to always invalidate rather than diff draft membership before/after (documented trade-off: a false-positive re-invalidation on an already-in-group re-click costs one redundant rebuild, avoiding diff complexity -- the status wording's separate claim that this also cost a redundant RE-CONFIRM was inaccurate and corrected same-day, see the 2026-07-20 invalidation-status-honesty entry below). Consolidated onClearSeeds's duplicated inline partitionGeneration++ + invalidation logic to call the shared function instead of running both independently. onToggleSeedPickMode/onProposeGroups/onAssignSelectedPatchToGroup/window.__skin.proposeGroups/window.__skin.assignPatchToGroup already called the shared function from a prior round, no changes needed there. Verified against real CoinSRF (141 coins, samples/CoinSRF_yohaku-skin-plate-20260717.recipe.json) using only real coordinate clicks (window.__skin used solely to observe workerRunning/partitionResult, never to drive actions): confirmed A#495/B#425 (72/69), started a real resolution-96 physical build (workerRunning:true), then mid-build moved a patch via a real click on '選択中のパッチをAへ' -- workerRunning immediately flipped to false, partitionResult stayed null, the exact alert-equivalent status text appeared, the real step fell back to 6 (visualReviewed/manualReviewed stayed true), and a 3-second re-check confirmed no late Worker result was ever adopted (hasResult/workerRunning both still false, terminated Worker's message never arrives). Re-confirming afterward restored confirmed:true and a fresh build-Worker start (workerRunning:true again on the new 72/69 draft), immediately cancelled without waiting for completion -- CoinSRF's heavy physical build was not run to completion this round, only started and discarded twice for verification. P1: added pure normalizeDisplayedStep(actualStep, displayedStep), resetting the browsed displayedStep to null (follow) whenever the real workflow regresses below it (e.g. reselecting endpoints while reading a past step via 前へ) -- called from refreshPartitionTutorial() to reset the persistent tutorialDisplayedStep module variable itself, not just clamp deriveTutorialNavState's one-shot return value (clamping only the output would leave the stale module variable to re-trigger the same wrong 'viewing past' reading on every subsequent render). Added 5 new partitionTutorial.test.ts assertions covering the instruction's three regression cases (actualStep=2/displayedStep=6 -> null; actualStep=7/displayedStep=6 -> unchanged; actualStep=6/displayedStep=6 -> follow) plus null-input and equal-step edges. Partition tests 70 (41+29), test-source typecheck, eight-page npm run build (Worker chunk included), git diff --check all passed. Not committed; not deployed (author review pending)."
    },
    {
      "date": "2026-07-19",
      "note": "Post-limit audit fixes Phase B (v0.10, docs/next-agent-instruction-20260719-post-limit-audit-fixes.md): the guide did not change partition math, gate thresholds, or coin shapes. B1: added pure draftMatchesConfirmedPartition(draftA, draftB, confirmed) (set comparison, array order irrelevant, A/B swap counts as a mismatch); tutorial snapshot.confirmed now reflects this instead of state.partition!==null, and buildPartition() itself re-checks it before starting the Worker (alert 'A/Bを変更したため、もう一度「確定」してください', no Worker start on mismatch). propose/manual-assign/reseed now invalidate any stale partitionResult + export links via invalidateStalePartitionResult(). B2: added deriveTutorialNavState(actualStep, displayedStep, flags) and a session-only tutorialDisplayedStep, decoupling 'which step is being read' from derivePartitionTutorialStep's real workflow position -- 前へ/最初から読む only move the displayed page (never geometry/history/draft/review flags); 次へ while browsing a past step only turns the page and never sets visualReviewed/manualReviewed, which can only change at the real step 4/5; added a 現在の工程へ戻る(Step N) control and a 表示中/現在の工程 note while browsing; highlights are suppressed while browsing since they point at the real step's controls. Verified against real CoinSRF (141 coins) with real UI clicks (canvas endpoint picks, propose, confirm): editing the draft after confirming (moving one patch via a real click) flipped draftMatchesConfirmed() to false and the real step to 6 while visualReviewed/manualReviewed stayed true; the build button was blocked with the exact alert text and workerRunning stayed false; re-confirming restored step 7 and a real build-Worker start (immediately cancelled, the heavy compute itself was not repeated); 前へ from step 7 visibly showed Step 6/8 with the viewing-past note and 現在の工程へ戻る(Step 7), confirmed by screenshot; 最初から読む from step 7 set displayedStep=1 while actualStep/confirmed/review flags/draft counts were provably untouched. B3: tsx added as a direct devDependency (was only reached via npx, which hit the registry and failed offline with ENOTFOUND) and test:partition no longer uses npx; ran npm run test:partition with no network and confirmed all tests still pass from the local binary. Added 13 new partitionTutorial.test.ts assertions (65 total across both partition test files), test-source typecheck, eight-page npm run build, git diff --check all passed. localStorage still only persists open/visualReviewed/manualReviewed (displayedStep is an in-session module variable), old stored values remain compatible. Not committed; not deployed (author review pending)."
    },
    {
      "date": "2026-07-19",
      "note": "T13 connected two-way split gate (v0.9): after the graph-distance bisection, same-colour components not reachable from the corresponding A/B endpoint are moved to the opposite side. Normal export now additionally requires savedTopologyA/B.connectedComponents === 1; disconnected results remain available only through the explicit verification/non-passing export. Added star-graph island-repair and disconnected-gate regressions. Partition tests 41, test-source typecheck, eight-page npm run build and git diff --check passed. The heavy real CoinSRF physical build was not repeated; no claim that the automatic repair reproduces the author's prior final 67/74 override. Uncommitted and undeployed."
    },
    {
      "date": "2026-07-19",
      "note": "T13 two-ended split proposal (v0.8): old seed-connected-component proposal produced 137/4 on dense CoinSRF and did not match the author's intended 'select both ends, split roughly in half' interaction. UI now records first click as A endpoint and second as B endpoint; proposeGroupsBetweenEndpoints ranks patches by the difference of weighted graph-geodesic distances from those endpoints and bisects the ordered list (141 patches => 71/70), with Euclidean fallback for disconnected patches. Manual A/B override, confirm/history seed order, Worker physical split, Float32 topology gate, and export remain unchanged. Added 6-node-chain 3/3 regression; partition tests 38, test-source typecheck, npm run build, git diff --check passed. Local confirmation UI opened; real CoinSRF color-shape confirmation, physical mesh build, commit and deploy intentionally pending author review."
    },
    {
      "date": "2026-07-19",
      "note": "T13 winding/volume final (v0.7): polygonizeTetのtet内勾配による局所orientに加え、binary STLと同じFloat32頂点同一性で隣接面を連結成分ごとに整合させるorientMeshForSavedStlを追加（座標・Scaleは不変）。作者が実CoinSRF 141 coinsを実UIで137/4へ分割し、resolution 96を31.8秒で完走。original/A/Bは共通Scale 21.506563、全てclosed・windingConsistent・degenerateFree・1 component。体積35627.54/35053.36/570.71mm3、A+B差3.47mm3=0.01%。実メッシュ重複/未割当/inconsistentは各0件、Wilson 95%上限0.43%/0.43%/0.45%で仮許容1%内、通常ゲート合格。通常4ファイルを実保存しSTL SHA-256がprovenanceと一致。Optimizer 0.8.2 --quick scale=1.0でもA/B双方watertight、境界辺0、非多様体辺0、1 shellを確認。partition tests 38件・test source typecheck・npm run build・git diff --check通過。未コミット・未deploy。"
    },
    {
      "date": "2026-07-19",
      "note": "T13ゲート訂正 (v0.6, codex-instruction-20260719-katachi-ab-gate-correction.md): 2nd independent audit rejected v0.5's 'gate pass' as a real completion -- P0-1 rescaleMeshResult() forces original/A/B onto ONE common scaleMmPerUnit (derived once from original, verified at runtime via gate.commonScale, not hardcoded to the 'original' field name). P0-2 inspectSavedStlTopology() judges the mesh AS ACTUALLY SAVED (Float32-rounded via Math.fround, matching encodeBinaryStl's DataView.setFloat32 exactly) -- boundary/non-manifold/degenerate (explicit, not silently excluded)/connected-components, plus a NEW winding-consistency check (directed-edge count) that v0.5's inspectWatertight could not see; real CoinSRF found BOTH parts non-watertight post-save (part-A: 132706 winding-inconsistent edges; part-B: 2532) despite passing the old Float64 in-memory check -- this is what v0.5 recorded as an unexplained Katachi/Optimizer discrepancy; Optimizer's independent diagnosis of the same bytes (winding_consistent:False) now agrees. P0-3 evaluatePartitionGate() computed once inside buildPartitionMeshes (Worker), UI/provenance read the same object; folds in volumeDiff and inconsistent (previously display-only) using the 95% UPPER BOUND for every Monte Carlo quantity, and fails closed on non-finite/zero original volume. P1-1 wilsonUpper95() replaces the normal-approximation CI (which reported margin=0 at k=0, silently treating 'zero found in a finite sample' as proof of zero); real CoinSRF at n=20000 gives ~3.00% upper bound at k=0 (nonzero, honestly reflecting under-sampling of a shape occupying only ~4.4% of its bounding box) instead of the old 0%. P1-2 main.ts's buildPartition() now terminates the Worker on every exit path (success/error/stale -- previously leaked on success) and terminates IMMEDIATELY on a stale-generation PROGRESS message (previously let a doomed computation run to completion); partition.worker.ts measures elapsed time from build-request receipt, not module load. picking.ts's raymarchComposite was made robust to compositeSdf's non-Lipschitz-1 smooth-boolean composition (a plausible root cause of v0.5's real-click failure on the dense 141-coin packing): damped sphere trace (STEP_DAMPING=0.75, 400 iterations) plus a fixed-step linear-scan+bisection fallback -- verified end to end with REAL canvas-coordinate clicks only (no window.__skin fallback, per this round's explicit prohibition): seed selection (#425, #523) and the resulting 137/4 A/B split reproduced identically across two independent full runs. Found and fixed a real, previously-undetected bug in src/lib/geometry/pointInMesh.ts's buildInsideTester while writing unit tests: its spatial-hash bucket-assignment loop used unclamped bbox-derived indices, so a triangle whose bbox touched the grid's outer edge got pushed into the same bucket multiple times, double/quadruple-counting its ray hit and flipping the inside/outside parity -- reproduced on a unit-cube fixture, fixed by clamping before the loop; this function is what partition.ts's verifyMeshPartition (the P0-3 gate's own fidelity measurement) depends on, so this was in-scope, not tangential. Added src/studies/skin/partition.test.ts (19 assertions, run via `npx tsx`, no test framework added per AGENTS.md 'no heavy frameworks' -- gate cases, common-scale regression, float32-rounding winding-defect rejection, point-in-mesh edge/vertex/seam stability incl. the bucket bug above; Worker lifecycle explicitly NOT covered, Vite's module-Worker import doesn't run under plain Node -- documented in the file, verified by hand instead against the real build). Final real-CoinSRF gate result: FAIL (non-watertight post-save + 95%-upper-bound overlap/gap/inconsistent all at 3.00% > 1% tolerance; volumeDiff 0.24% OK) -- exported only the verification-labelled 4-file bundle (yohaku-skin-partition-UNVERIFIED-20260719-053530-part-{a,b}.stl etc., sha256-verified byte-identical via independent shasum), did NOT provide a normal pass bundle, and did NOT delete/overwrite the prior round's real ~/Downloads/yohaku-skin-partition-20260719-* files. Per the instruction's explicit completion condition, this round's status is UNVERIFIED, not complete/deployable. Not committed (author review pending); deploy deferred pending explicit author approval. npm run build (tsc -b && vite build, all 8 pages) passed."
    },
    {
      "date": "2026-07-19",
      "note": "T13監査修正 (v0.5, codex-instruction-20260719-katachi-ab-audit-fixes.md): P0-1 real mesh fidelity -- new src/lib/geometry/pointInMesh.ts (Möller-Trumbore ray-triangle extracted from mpm/stlImport.ts, shared, no behavior change) + buildInsideTester (Y/Z-bucketed) let partition.ts's new meshFidelity sample the ACTUAL meshA/meshB/originalMesh triangles instead of re-evaluating analytic fields (kept as fieldConsistency, relabeled). P0-2: cloud-sculpt/meshExport.ts's new buildMeshesFromSharedField samples dOriginal/dA/dB once per grid corner (was up to 7x); buildPartitionMeshes now runs in a new partition.worker.ts (Vite module worker) -- measured main thread stayed responsive during a 49.1s real build (real CoinSRF, 141 coins, resolution 96); cancel and double-invocation guard verified. P0-3: export gate now requires BOTH watertight AND meshFidelity overlap/gap within 1% of original volume (仮決め); verified rejecting an out-of-tolerance case (28.93% gap at resolution 32) and passing a real one (CoinSRF at resolution 96, 0.000mm³ overlap/gap/inconsistent). File names fixed to <base>-part-a.stl/-part-b.stl/-partition.recipe.json/-partition-provenance.json; provenance SHA-256 (input recipe + both output STLs) verified byte-identical to real downloaded files via independent Python hashlib. Real CoinSRF recipe (samples/CoinSRF_yohaku-skin-plate-20260717.recipe.json, sha256 526246cb... confirmed) loaded via the real file input: 141 coins/ID 392-532 confirmed, 266 real adjacency edges, seed-based propose from coin #453 gave a 137/4 split (packing is mostly one connected blob at gap=-0.15, not a bug), manually rebalanced to 69/72 and confirmed/built/exported for real. 3D click-to-select-a-patch could NOT be verified with real coordinates on this dense packing this session (pointer events fired but raymarchComposite found no hit at several coordinates including canvas center) -- seed selection was instead driven via window.__skin's debug entry points (same underlying functions a real click calls). Optimizer CLI diagnosed the real part-a/part-b outputs (same --quick preset, scale 1.0) and found a real, unresolved discrepancy: Optimizer's own topology check reports non-watertight/13-14 shells for both parts, contradicting Katachi's own inspectWatertight (watertight, 1/3 connected components) -- root cause (likely float32 STL round-trip vs Katachi's internal Float64 quantization) not yet confirmed. wrangler whoami confirmed authenticated (a@satw.jp) but deploy deferred pending explicit author confirmation (public-facing action). Author's raw print observation recorded verbatim in README with a separately-labeled Hypothesis section. npm run build (all 8 pages) passed."
    },
    {
      "date": "2026-07-19",
      "note": "T13 'coin由来A/B分割' (v0.4): buildPatchAdjacency extracted from estimatePatchComponents (same touch threshold, no regression) as first-class edges; proposeGroupsFromSeeds grows a seed-connected A candidate via that graph, author overrides per-patch, confirmPartition records full groupA/groupB/seedIds to history (replay-reproducible, no RNG re-run). buildPartitionMeshes (new src/studies/skin/partition.ts) computes an ownership field max(dOriginal, dA-dB) per side (exact boolean intersection with the true composite, not smoothMin) so A/B are complementary by construction; verified overlapVolumeMm3=0.000 and gapVolumeMm3=0.000 (Monte Carlo, 20000 samples) on a real 51-patch packing at resolution 32, both parts watertight. Two-color bead preview (GROUP_A/B/UNASSIGNED_GROUP_COLOR) plus a one-touch A+B/Aのみ/Bのみ filter (InstancedMesh zero-scale hide trick) verified via real ref-based coordinate clicks (3 screenshots, no console errors). 4-file export (part-a.stl/part-b.stl/.recipe.json/-provenance.json) verified by hooking document.createElement; provenance always carries both sides' metrics regardless of which STL(s) were exported. Measured, documented limitation: buildPartitionMeshes at the default resolution (96) with 51 real patches exceeded the verification tool's 30s timeout twice (actual duration unmeasured, presumed 60s+); resolution 32 completed in 1.0-2.0s. Root cause: dOriginal/dA/dB are each independently re-evaluated per grid point (no shared sampling pass), so cost scales with gridPoints x totalPatchPoints x 3. No live progress/elapsed display was implemented (this codebase has no Worker/async-chunking infrastructure yet) -- status only shows a static '計算中...' message before the (synchronous, blocking) call, honestly flagged as a gap rather than faked. npm run build (tsc -b && vite build, all 8 pages) passed."
    },
    {
      "date": "2026-07-17",
      "note": "T18 design alignment: unified UI chrome with satw.jp paper/ink/hairline tokens and Helvetica Neue while preserving the dark 3D viewport and skin instrument colors. Awaiting author visual approval before deploy."
    },
    {
      "date": "2026-07-17",
      "note": "T16 style consolidation: moved byte-equivalent shared chrome rules to src/styles/base.css while keeping Skin-specific coverage instruments, patch controls, and mesh styles local. Build output CSS size remained 3.88 kB."
    },
    {
      "date": "2026-07-17",
      "note": "T16 UI consolidation: migrated Skin numeric sliders to shared src/lib/ui/slider.ts while preserving integer/three-decimal formatting; build and real-coordinate hit testing passed on verify port 5185."
    },
    {
      "date": "2026-07-17",
      "note": "T16 UI consolidation: shared Version/UpdatedAt row with identical DOM and placement; build passed."
    },
    {
      "date": "2026-07-17",
      "note": "T16 input/loop consolidation. Build passed; real-coordinate surface click added and selected patch #52, increasing patches 51 to 52 and history 2 to 3. Existing capacity auto-switch to beads preserved."
    },
    {
      "date": "2026-07-13",
      "note": "T11 v0.2 'リングの皮': added Patch.shape (coin/flatRing/ring3d). Coin path is the exact T10 logic factored into buildCoinPoints (behavior unchanged, verified: fresh coin pack still produces the same kind of irregular blob patches). flatRing (buildFlatRingPoints) places ringNodeCount nodes on a tangent-plane circle of radius anchorR*holeRatio with node radius anchorR*(1-holeRatio)*0.9 -- a real hole opens once holeRatio exceeds ~0.47, below that it reads as a coin (仮決め, no principled derivation, documented in README). ring3d (buildRing3dPoints) reuses S-rings' generateRingBalls/rotatePoint (../rings/ring.ts, imported not copied -- T11's '最大限共有'): ring center = anchor + normal*tubeR, axis = normal, giving a torus tangent to the host's local tangent plane along the full circle of radius R (elementary tangency condition, derivation in field.ts doc comment); random phase applied post-generation via rotatePoint since generateRingBalls itself has no phase parameter. generateShapePoints is the shared dispatch used by both packPatchesGreedy and main.ts's manual-add click handler. gap's UI range widened to [-0.3, 0.3] (T11 §2 'パッキング間隔つまみに負側') -- the underlying formula (clearance - gap) was untouched, a negative gap simply enlarges the allowed radius, permitting deliberate overlap so ring tubes can pass through neighboring rings' holes. compositeSdf's plate-mode branch now splits by shape: coin/flatRing patches still intersect with the shell band (T10-identical, verified no regression), but ring3d patches are unioned RAW (never intersected with the shell) because a ring only touches the surface along one circle and would be sliced to a sliver otherwise -- this means 'plate mode + only ring3d patches' automatically satisfies T11 完了条件3 ('ホスト表示 off でも形が立ち上がる') without a separate visibility toggle: verified via screenshot, the host's egg silhouette read purely from a cluster of 111 tangent rings with zero shell geometry drawn. Window mode is unaffected (any shape still just carves the shell). shaders.ts replicates the identical split in GLSL: the existing uPatchData[i].y owner-index uniform slot now also encodes shape via a +0.5 fractional offset (isRingPoint = fract(y) > 0.25) instead of adding a 4th uniform array, since this Study already hit the fragment uniform-vector budget once at T10. New linking.ts instrument reuses rings/linking.ts's gaussLinkingNumber directly (a ring3d patch's points array, in generation order, already IS the closed centerline polyline the function expects) plus a local adjacency heuristic (candidate pairs = closest point-pair clearance within 3x patch size, 仮決め) and union-find for a connected-component count; also findDeepPatchOverlaps (fusion warning, same 0.55 threshold and caveat as rings' findDeepOverlaps, generalized to any two patches not just rings). meshExport.ts's sampling-bounds padding replaced a thickness/2-only heuristic with bounds computed directly from every patch point's actual position+radius (exact, strict superset of the T10 coin-only case) since ring3d patches can extend well past the shell band. UI added: shape selector (3 buttons), ring-specific slider group (RING_SPECS: flatRingHoleRatio/ringNodeCount/ringTubeR/ringWobbleR/ringWobblePos), and a 絡み計器 gauge panel (linked/adjacent pair ratio, component count, overlap warning count). Verified end to end (isolated Chrome port 5185, real PointerEvent clicks after elementFromPoint hit-test + scrollIntoView): coin unregressed (51 patches, shape all 'coin'); flatRing O-shaped windows visible in window-mode screenshot; ring3d with gap=-0.12 packed 111 patches, plate-mode screenshot showed the host's egg silhouette with ZERO shell rendered (完了条件3 confirmed visually); linking gauge on that packing reported 401 adjacent / 58 linked pairs / 63 components (raw Lk values from -5.05 to 3.00, i.e. some multi-wrap links, not just ±1); mesh inspection at resolution 80 gave plate mode 91,376 triangles watertight (0 open/non-manifold edges) but 191 connected components (more than the live gauge's 63 -- an HONEST discrepancy recorded in README, hypothesized cause: individual rings' own nodes can fragment at this resolution/roundK, a NEW failure mode beyond T10's cross-patch fusion gap, left as unresolved in Next) and window mode gave 189,032 triangles watertight with the shell staying in 1 connected piece; history export->import round-tripped byte-for-byte (16 entries, JSON string match modulo exportedAt) including the new shape/negative-gap data. All 7 pre-existing pages (index/gravity/sag/mpm/foam/rings/pack) verified console-error-free (no regression). npm run build passes with the Patch.shape now required, SkinParams' 6 new fields, and UiHandles.setGauges' extended signature."
    },
    {
      "date": "2026-07-13",
      "note": "T10 initial implementation: new Study S-skin. Host = S1 ball list + k (unchanged, imported from cloud-sculpt/field.ts, same as pack). Shell = |hostSdf| - thickness/2 (field.ts's shellSdf). Patch = an anchor point projected onto the host surface via SDF-gradient Newton descent (field.ts's projectToSurface) plus 3-8 sub-points scattered in the anchor's local tangent plane and re-projected, all smooth-min'd together with one roundK -- structurally just another Ball[]-like point list, no new distance math invented (only the placement -- points hugging a curved surface instead of floating in open space -- is new). Multiple patches are one flat union (patchesSdf), mirroring pack's void-field reuse of a single roundness knob for both intra-group blending and the boolean op with the host (仮決め, same precedent). Composite is mode-dependent: plate mode = opSmoothIntersection(shellSdf, patchesSdf, roundK) (殻∩パッチ群), window mode = opSmoothSubtraction(patchesSdf, shellSdf, roundK) (殻−パッチ群) -- both Quilez smooth booleans, written locally in field.ts (not imported from pack, keeping the Study self-contained same as pack did relative to foam). New op packPatches carries the resulting patch list (anchor+sub-points+radii) explicitly, mirroring pack's packVoids/mpm's freeze precedent -- replay never re-runs the greedy walk or the surface projection. New op setMode records the plate/window toggle itself, so mode survives export->import even though it doesn't touch the packing data (completion condition 5 explicitly wants full history reproduction). Manual patch add (click while in add-patch mode, host-only raymarch target, same simplification pack made) places a light 3-point spray rather than running the full irregularity/gap logic -- documented as not honoring gap, same as pack's manual void add. Gauges: estimateMortar (cross-patch point-pair clearance, same dual reading as pack's thinnest-wall proxy -- 'thinnest mortar' in window mode, 'closest two plates came to touching' in plate mode), estimateCoverage (two stacked Monte Carlo approximations: shell-band volume as a proxy for surface area, then patch-membership fraction within that), estimatePatchComponents (a FAST proxy -- union-find over patch-to-patch point proximity, meaningful only for plate-mode 'how many patches fused into how many pieces', explicitly documented as NOT modeling window-mode shell connectivity). The AUTHORITATIVE connected-component count T10 完了条件3 asks for is computed separately, in meshExport.ts's countConnectedComponents, via union-find over the actual generated triangle soup's shared vertices (same vertex-quantization convention as cloud-sculpt's inspectWatertight) -- this is the number the README's completion-condition verification cites, not the live proxy. Mesh export pads the host's own sampling bounds by thickness/2 on every side (the shell band extends that far past the host's raw ball-radius envelope) before delegating to buildMeshFromField. Added nav links to/from all 7 existing pages. Two real bugs found and fixed during isolated-Chrome verification (port 5185), both from the same root cause: the first GLSL draft (768-point uniform arrays for host+patch pos/radius/owner) exceeded MAX_FRAGMENT_UNIFORM_VECTORS(1024) on the verification renderer, fixed by lowering HOST_MAX_BALLS 128->96 and PATCH_MAX_POINTS 768->256 and folding patch radius+owner into one vec2 uniform array (uPatchData) instead of two separate float/int arrays. Second, and more substantive: real-coordinate click verification (AGENTS §3) found that click-to-select silently failed on every plate-mode patch -- picking.ts's 'which patch owns this point' test used a zero-surface-distance threshold (correct for window mode, where only a carved opening's rim belongs to a patch) but plate mode's visible surface is shell∩patches, so almost every rendered plate-face point sits deep inside a patch's point spheres, never near their own zero surface. Fixed by making picking.ts's nearestPatchId (and the equivalent shader onPatch/coloring logic) mode-aware: plate mode picks the nearest patch unconditionally (no threshold), window mode keeps the rim-only threshold test. Verified after the fix: real PointerEvent click selected a plate patch (\"選択中: パッチ #38\"), real Delete keypress removed it (44->43, removePatch recorded)."
    },
    {
      "date": "2026-07-13",
      "note": "Added the full-mesh overlay view (raymarch is capped at ~160 patches/256 points by the uniform budget and silently under-draws dense packings — the author read a fully packed ring skin as sparse). Also authored recipes/ring-cover-20260713.json: 869 rings in two greedy passes fully clothe the host (2,578 interlinked pairs, 31 components)."
    },
    {
      "date": "2026-07-13",
      "note": "T12 'skin の生きた表示' (v0.3): two author reports -- (1) flatRing/ring3d selection didn't turn the highlight color despite picking working; (2) raymarch-capacity-limited views weren't interactive enough, mesh-overlay checking was heavy. Investigated report (1) against the suspected root cause in the task brief (owner-index fractional encoding for ring3d, uPatchData[i].y = pi + 0.5, compared against uSelectedPatchOwner) and found shaders.ts's nearestPatchOwner() already floors correctly (int(floor(uPatchData[i].y + 0.01))) -- verified empirically in isolated Chrome (port 5185) that highlighting DOES work correctly for all three shapes when the selected patch's points fall within PATCH_MAX_POINTS (256). The actual root cause: flatRing/ring3d patches have far more points per patch (ringNodeCount~10) than coins (4-9 sub-points), so a typical pack (50-110 patches) blows the 256-point uniform budget almost immediately -- patches beyond the budget are never uploaded to the GLSL uniform arrays at all, so CPU-side picking (picking.ts, unbudgeted) finds and selects them but the GPU-side highlight has no geometry to color, explaining the author's precise phrasing ('picking seems to work, but it doesn't turn color'). Also fixed a second, independently-found bug: clicking a patch-shape button (コイン/平リング/立体リング) recorded the state change but never updated the button's own mode-active CSS class (main.ts's onSkinParamChange callback only called record()+afterMutation(), never ui.ts's renderShapeButtons) -- added a lightweight ui.setPatchShape(shape) called only on patchShape changes (full syncSkinParams, which re-renders every slider, stays reserved for history replay). Built the bead approximation view (renderer.ts's updateBeads/updateBeadSelection/setViewMode): THREE.InstancedMesh spheres for every host ball (translucent backdrop, opacity 0.16) and EVERY patch point with no uniform-array cap, using instanceColor (setColorAt, no geometry rebuild) for the existing cross-Study selection color (vec3(1,0.75,0.2) -- kept instead of inventing a literal blue per AGENTS §5's 'pick one scale and don't change it'; the author's 'turns blue' is read as colloquial for 'gets highlighted'). Picking/manual-add reuse picking.ts's raymarchComposite/raymarchHost unchanged (verified working in bead mode too). main.ts gained a three-way viewMode ('raymarch'|'beads'|'mesh') replacing the old boolean overlayActive, with an auto-switch rule in afterMutation(): once total patch points exceed PATCH_MAX_POINTS while on raymarch, switch to beads and show a one-shot 'auto-switched' banner (ui.ts) -- retiring T11's silent-truncation raymarch-only default, which the task brief explicitly called out as the root of the 'looks sparse' misreading. Mutating state while on 'mesh' drops back to raymarch/beads (mesh-invalidation rule generalized from T11's boolean version). Also gated the raymarch-only capacity warnings in updateSelectionLabel (host/patch/point count caps) behind viewMode==='raymarch' -- showing them in beads/mesh mode would describe a limit that view doesn't actually have (AGENTS §6). Verified end to end: recipes/ring-cover-20260713.json (869 rings, 6,952 points) auto-switches to beads on import and renders as one coherent mass; direct render() benchmarking (window.__skin.benchmarkRender(n), added because the automated verification tab reports document.hidden=true which throttles/pauses the on-screen rAF-based fps counter to ~0) measured ~0.30ms/frame average over 150 calls at that scale -- a CPU-submission-cost lower bound, not a vsync-inclusive real fps; the actual on-screen smoothness on the author's own machine is left as a visual-confirmation item, honestly flagged in README rather than claimed from this measurement. All 8 pre-existing pages verified console-error-free; STL inspect/export, mesh-overlay toggle, and history export/import round-trip all re-verified with no regression. npm run build passes."
    },
    {
      "date": "2026-07-26",
      "note": "R2 — first Library promotion (Optimizer/docs/sonnet-instruction-20260726-katachi-r1-r2-library-first-extraction.md). Author approved Gate A Q1 (src/lib is the canonical Library), so sha256Hex became the first promoted operation: src/lib/hash.ts is now the single implementation. Equivalence was MEASURED, not assumed - both former implementations (skin's ArrayBuffer|string version and interior-growth's ArrayBuffer version) were reproduced and compared over 9 cases (empty input, \"abc\", non-ASCII string, bytes containing 0x00, 200,000 bytes): byte-identical output in every case. Real-browser check with hit-tested real-coordinate clicks, no console errors. Tests: test:interior-growth 106 -> 112 (6 known-value cases added, expected values taken from published SHA-256 vectors rather than generated by the same implementation), test:partition 102 unchanged, tsc --noEmit clean, build 9 pages. Version deliberately left unchanged: the version-numbering rule is Author decision Q8 and is still undecided, so no new rule was invented here. skin: the private sha256Hex in main.ts was removed and src/lib/hash.ts imported; the ArrayBuffer|string signature is kept because this Study also hashes imported recipe TEXT (importedRecipeSha256). Call order, save order, UI text and the hash written into provenance are unchanged. Browser: grow re-ran (host balls 12), STL export path reachable."
    }
  ]
}
