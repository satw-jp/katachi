{
  "id": "foam",
  "title": "泡のセル (Foam Cells)",
  "status": "active",
  "version": "0.1.0",
  "createdAt": "2026-07-11",
  "updatedAt": "2026-07-17",
  "params": [
    "opening",
    "thickness",
    "count",
    "radiusBase",
    "radiusSpread",
    "k",
    "seed"
  ],
  "related": ["cloud-sculpt"],
  "revisits": [
    { "date": "2026-07-17", "note": "T18 design alignment: unified UI chrome with satw.jp paper/ink/hairline tokens and Helvetica Neue while preserving the dark 3D viewport and scene colors. Awaiting author visual approval before deploy." },
    { "date": "2026-07-17", "note": "T16 style consolidation: moved byte-equivalent shared chrome rules to src/styles/base.css while keeping Foam-specific cell and mesh styles local. Build output CSS size remained 2.75 kB." },
    { "date": "2026-07-17", "note": "T16 UI consolidation: migrated Foam numeric sliders to shared src/lib/ui/slider.ts while preserving integer/three-decimal formatting; build and real-coordinate hit testing passed on verify port 5185." },
    { "date": "2026-07-17", "note": "T16 UI consolidation: shared Version/UpdatedAt row with identical DOM and placement; build passed." },
    {
      "date": "2026-07-17",
      "note": "T16 loop consolidation: migrated the requestAnimationFrame shell to src/lib without changing foam field or history semantics. Build passed; a real-coordinate click on Grow increased history from 3 to 4 and rendering continued at the known heavy roughly 1fps on verify port 5185."
    },
    {
      "date": "2026-07-11",
      "note": "T7 initial implementation: new Study S-foam. Cells decomposed from the shared field.ts ball list via nearest/second/third-nearest ballSdf (a radius-following distance without the extra power-distance tuning constant). foamSdf (cell.ts) unions a Voronoi wall + outer shell inside the cloud, then carves an opening via a Plateau-border-preserving 'edge distance' so the opening knob sweeps 0=closed cell shells -> 1=thread-only Plateau network continuously. Raymarch shader (shaders.ts) mirrors cell.ts exactly with a damped step (non-exact SDF near holes). meshExport reuses cloud-sculpt's marching-tetrahedra core via a new buildMeshFromField() extraction (S1's buildCloudMesh behavior unchanged, verified by re-reading the refactor). Own history/recipe format (studyId 'foam'), S1 recipe import via cloud-sculpt's own replay() read-only. Added nav links to/from S1/S2/S2b/S2c (existing Study files touched only for nav-link + vite entry, per task instruction)."
    }
  ]
}
