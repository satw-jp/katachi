# OPT-LD-1 READINESS / CANDIDATE

## Actual Hikari CPU ShapeSource receiver comparison checkpoint — 2026-08-09

`shape-source-reference-harness.html` is a deliberately bounded comparison checkpoint. It traces the real shared Hikari `ShapeSource` through `OpticsLayer.runCpuShapeSourceReferenceCase`, which uses the existing CPU receiver transport with a fixed 16,384-sample diagnostic override and does not publish, build scene geometry, invoke callbacks, or use WebGPU. It is not SAFE-mode performance evidence.

The frozen literal fixture records the single `grow` recipe from `docs/hikari/cases/hikari-blender-backlight-study.hkr` (document/app v0.29.2, commit `b8f7b50`; `k=.6`, `count=12`, `radiusBase=.7`, `radiusSpread=.5`, `seed="yohaku"`). The test replays that recipe and compares centers/radii/smoothness without legacy ball IDs. Frozen settings begin with `DEFAULT_HIKARI_SETTINGS`, set optics/manual daylight/−24°/width 1/no inclusion/fixed diagnostic seed, and hold every value constant except sun size across `0.53°`, `5°`, and `20°`.

Each panel is sequential and has a frame yield, but there is no field cache, renderer, listener, animation, retry, GPU path, or per-panel normalization. It draws deposited RGB flux divided by texel area with one shared exposure and tone map in the production `512²`, `[-16,16]²` receiver frame. The printed values are integrated deposited RGB, in/out-domain deposit counts, and energy-ledger closure residuals. The small ball projection is explicitly explanatory only; it is not a renderer.

The page’s scope is intentionally narrow: **Actual Hikari CPU ShapeSource receiver behavior; comparison checkpoint only; not analytic/product pixel parity; not OPT-LD-2/LD3 GO or acceptance; not WebGPU/Blender/physical validation.** Absence of obvious softening is valid. It makes no 17×17/effective-area metric, monotonic pass/fail result, or promotion claim.

## ShapeSource gesture bridge — harness-only candidate

`shape-gesture-bridge-harness.html` is a static four-state author checkpoint: frozen yohaku `OFF`, plus a single connected local five-ball thickening at `LEFT`, `CENTER`, and `RIGHT`. It uses the actual CPU `ShapeSource` diagnostic seam at fixed `0.53°` / `16,384` samples, preserving the production `512²`, `[-16,16]²` receiver frame. OFF is the exact frozen fixture object. For the three ON states, the largest base ball (lowest index on a tie) defines `R`; the anchor moves only in receiver-right x, and the first upward base-SDF exit is selected with a fixed 256-interval scan followed by 64 bisections. The initial upper search bound is conservatively `max(cy+r) + (N−1)k/4 + R`, accounting for the smooth-min lowering bound.

Each ON bridge appends exactly five frozen balls (`ρ=.20R`) with `0.18R` z spacing, so adjacent balls overlap and the central top extends `.15R` above the sampled base root. The page runs the four cases sequentially once with a CPU-only `OpticsLayer`, renders each canvas once, retains no receiver-field arrays, and lets the selector highlight one already-rendered frozen card. The body canvas is an explanatory projection of constituent balls, not the actual smooth-union body renderer. It draws common-exposure receiver light and a zero-centered signed ON−OFF view whose single scale is fixed beforehand as `1 / commonExposure`; cyan marks added light and orange removed light.

This is explicitly a harness-only bridge candidate: **not continuous motion / LD3; not persisted/shared renderer/WebGPU/Blender/physical; not OPT-LD-1/2/3 GO or acceptance.** There is no slider, adaptive exposure or geometry tuning, retrace, retry, animation, promotion, or acceptance gate. A subtle, blank, or no-line receiver result is valid evidence.

## Existing analytic LD1 readiness record

**NOT OPT-LD-1 GO OR ACCEPTANCE.** **LOCAL MOTION / FORM EXPLORATION ONLY · OPT-LD-2 HAS NOT BEEN COMPLETED · NOT OPT-LD-3 GO OR ACCEPTANCE.** **CPU-ONLY.** **NOT SHARED ShapeSource / WebGPU / Blender.** **NOT PRODUCTION.**

This standalone study is bounded readiness evidence only: the same deterministic canonical finite-light sample buffer is traced through a flat relief plate and through one selected real analytic upper-height form. The small, non-persistent exploratory vocabulary is `single-bulge`（ひとつのふくらみ）, `connected-ridge`（連なる稜線）, and `pinch-valley`（つまんだ谷）. It does **not** claim canonical authored ShapeSource support. The path is air → material at the upper height surface, then material → air at a selected analytic far surface, using finite Snell refraction, Fresnel throughput, and fixed Beer–Lambert RGB absorption. Each resolved receiver hit is deposited once through the canonical `ReceiverTransportField` API.

The finite far-surface comparison is `flat`（平らな出口, exact historical default）, `following`（形に沿う出口, lower height `L=r` and constant **vertical** thickness `0.34` at corresponding x/z）, and `opposing`（反対向きの出口, `L=-0.5r` and vertical thickness `0.34+1.5r`）. Following surfaces are not claimed to have constant shortest/normal thickness or to cancel refraction exactly: an actual ray usually exits at a different x/z from where it entered. Opposing surfaces increase thickness variation; they do not promise that every light trace becomes stronger.

For the connected ridge only, `RIDGE POSITION / 稜線の位置` (`[-.18,.18]`) and `RIDGE BEND / 稜線の曲がり` (`[-.20,.20]`) offer deterministic local form controls. The harness opens and resets to connected-ridge × opposing-exit at amplitude `.18` and neutral gestures; selecting any other form disables the controls and ignores their values exactly. This is a controlled readiness comparison, not an animation or a claim of monotonic motion: every slider input retraces the same finite samples with no temporal accumulation, reseeding, persistence, or changed material/receiver/exposure/display settings.

The receiver field is never painted procedurally. A common support mask is constructed exclusively from the identical samples' unobstructed straight receiver landings. After raw physical deposits, both OFF and ON receive the identical **fixed 2-texel energy-normalized reconstruction** through canonical transport reconstruction, followed by a common 3-texel containment allowance (2 reconstruction texels plus one support texel). This is display reconstruction; **not a validated finite-source PSF**. OFF and ON share source samples, receiver domain, IOR, material, exposure, and display scale. There is no temporal accumulation, memory, bloom, trail, adaptive reframe, peak normalization, or expressive redistribution.

`interface exit` is displayed only as an intermediate diagnostic. Terminal source-energy closure assigns each surviving portion once to reflected, absorbed, raw receiver-hit deposited, receiver escaped, TIR, or unresolved flux. The displayed accepted/rejected receiver values are support-containment subsets that partition raw deposited flux; they are not additional source-energy terminal buckets. Clipped-texel count compares the reconstructed field immediately before containment with the contained field, so ordinary reconstruction redistribution is not labeled as clipping.

Open `harness.html` through the repository's normal static/dev serving path. The page keeps a persistent candidate/not-production banner and shows compact form/far-side selectors, fixed-exposure OFF, ON, and signed ON−OFF diagnostic fields, top-down analytic vertical thickness, a fixed-scale two-surface `z=0` profile, and representative physically mapped ray provenance. Selecting either surface recalculates the same light through real intersections and normals; it does not paint a receiver-side mark. Cyan/red difference coloring is diagnostic only, not physical RGB.

The test file is intentionally named `ld1Reference.test.ts` and the page labels remain `OPT-LD-1 READINESS / CANDIDATE` to avoid implying acceptance. Full LD1 acceptance still requires shared ShapeSource, CPU/WebGPU qualitative agreement, source-size response, and Blender/physical evidence; this study does not alter OPT-1c HOLD, Draft PR gates, GLOW-A3, or ART-2.

## Source-size readiness visual candidate — 2026-08-09

**Question.** With one fixed connected-ridge × opposing-exit analytic plate, does a physically corrected finite angular *diameter* source leave the same receiver trace in place while making it less concentrated as the diameter changes from `0.53°` to `5°` to `20°`?

**Setup.** `source-size-harness.html` is a standalone, no-control CPU checkpoint. It evaluates fixed primary and audit `16,384`/`32,768` runs at `512×512`, then displays only the common primary `32,768` receiver fields for the three diameters. Readiness metrics and both visual pages use the same flux-conserving triangular KDE of the raw physical deposit field at the frozen radius `8` texels. This is a resolution correction chosen before the attempt: `512 / 128 ×` LD1's existing radius `2` = `8`, so the `512²` field has LD1's `128²` world-space footprint. The active concentration is the independently precommitted full-field local concentration: maximum luminance in every complete grid-aligned `17×17` window divided by total full-field luminance, calculated with a summed-area table. `17 = 2×8+1`; pitch is `2.8/512 = 0.00546875`, so physical side is fixed at `0.09296875`. It has no ROI, crop, padding, display values, or adaptive footprint. Max-texel concentration remains diagnostic only. The retained LD1 `result.on.reconstructedField` stays radius `2` and is never repurposed as qualifying data. The LD2 primary/audit buffers are prefix-invariant 4D Sobol digital nets with an independent deterministic nested-uniform Owen scramble for each coordinate. Each scramble root comes only from the fixed seed plus `:ld2-primary` or `:ld2-audit` and the dimension; each output bit is flipped by a stable hash of that root, bit depth, and its already-scrambled prefix. Dimensions 0/1 fill the aperture square; dimensions 2/3 map to area-uniform disk `radius=sqrt(q)` and `angle=2πt`. There are no Cranley-Patterson shifts, rerolls, seed search, outcome-selected variants, or fallback estimator. The opt-in diameter branch leaves LD1's historical `sourceAngularRadiusDegrees=.53` slope calculation untouched. Its disk mapper uses `tan((diameter × π/180)/2)` in an orthonormal tangent basis around the existing fixed central direction. The oblique body is an analytic explanatory projection of the actual upper and opposing lower surfaces; it is not a ray-traced production body.

**Observation.** The page renders the primary nested-Owen `32,768`-sample radius-8 field. Active local concentration decreases (`0.018881405952925652`, `0.017726071711869337`, `0.00980127142721058`) and effective area increases (`50144.92`, `55818.90`, `82087.58`) for `0.53°`, `5°`, and `20°`. Primary raw receiver-hit RGB, support leakage, TIR, terminal closure, ray physics, sample buffers, and thresholds are unchanged.

The historical legacy-sampler `16,384` values (`2.1791e-4`, `1.3401e-4`, `7.4507e-5`; areas `33294.37`, `43921.41`, `65700.94`) are retained only as explicit failed negative evidence. They are not the primary Sobol page data.

**Interpretation / Hypothesis.** The visual relationship is consistent with the intended form → thickness → transmitted-light explanation: a wider source-direction disk integrates neighboring redirected landings into a broader, less concentrated version of the same trace. This is a hypothesis from this bounded analytic CPU study, not a production or physical-material conclusion.

**Limitations.** This is **NOT OPT-LD-1 OR OPT-LD-2 GO / ACCEPTANCE**; **FORMAL OPT-LD-2 HAS NOT STARTED**. It is CPU-only and not shared ShapeSource, WebGPU, Blender, or production. The active frozen local-concentration evaluator is **LOCAL CPU READINESS: QUALIFIED** with zero failures: its primary `20°` convergence ratio is `0.037355398802149484`, and primary/audit `20°` discrepancy is `0.0055354411310528295`, both below `5%`. This does not relabel or erase max-texel evidence: the pure radius-8 max-texel diagnostic replay preserves its exact two failures (`0.15190913729312328` primary `20°` convergence; `0.07365026383968129` primary/audit `20°` discrepancy). The pure radius-2 replay preserves the historical eight max-texel/effective-area failures without retracing. The legacy sampler also remains negative evidence. There is no source-size control, animation, accumulation, display blur, adaptive crop, per-panel normalization, or formal acceptance.

**Next.** The single precommitted radius-8 local-concentration attempt has been run once and qualified locally. Do not iterate radius, metric, estimator variants, seeds, thresholds, samples, or promotions here.

## Fixed 5° / 20° stability diagnostic — qualified local CPU verification

`stability-harness.html` is a separate, no-control local diagnostic page. On each page load it runs exactly the fixed primary `16,384`/`32,768` and audit `16,384`/`32,768` LD2 runs once and evaluates those exact four radius-8 objects once with the active local-concentration gate, reporting **LOCAL CPU READINESS: QUALIFIED** with zero failures. It separately performs pure max-texel and radius-2 metric replays over those same results to retain the radius-8 two and radius-2 eight negative-evidence failures without tracing again. It renders only the radius-8 qualification fields for `5°` and `20°` in row-major `{5°,20°} × {primary16,primary32,audit32}` order. Audit `16,384` is quantitative-only; no field from it is drawn.

All six receiver canvases retain the complete `[-1.4,1.4]²` 512×512 frame, unchanged `displayScale=9` and `exposure=1`, the existing physical display conversion, identical tone mapping, and one common CSS scale. The page separately exposes exact radius-8 max-texel two-failure and radius-2 eight-failure evidence, while the active 17×17 full-field local-concentration evaluator has zero failures. It maps primary16↔primary32 to the first/middle columns, explains that audit16↔audit32 is quantitative-only, and labels primary32↔audit32 solely as a visual independent-estimator comparison. A reader may compare the brightest knot, fine grain, and overall line as author observation only; this is never a qualification criterion.

This remains **CPU-ONLY**, **NOT PRODUCTION**, and **FORMAL OPT-LD-2 HAS NOT STARTED**. It makes no crop, zoom, normalization, blur after reconstruction, display adaptation, retry, control, animation, estimator change, or acceptance claim.
