# Katachi — 運用憲章（AGENTS.md）

このプロジェクトで作業するすべての AI・人間が従う。モデル非依存。
`$CODEX_HOME/AGENTS.md` の共通ルールを土台とし、この文書で Katachi 固有の規律を追加する。
同じ事項で本書の方が具体的な場合は、本書を適用する。

## 0. まず読む順

1. [STATEMENT.md](STATEMENT.md) — 原点。方針に迷ったらここへ
2. [RESEARCH.md](RESEARCH.md) — 骨子。用語（二つの余白・場・三つの手・三層）はすべてここで定義済み
3. この文書
4. 作業対象のタスク指示書（`docs/tasks/`）と、対象 Study の README + manifest

## 1. 不変則（どの段階でも変わらないもの）

- **比較 > 装飾**。並べて観察できることが、画面の美しさに優先する
- **正直な計算**。余白の推定には必ず根拠と精度の限界を添える。分からないものを分かった顔で表示しない
- **記録 > 記憶**。観察・破壊試験・人の反応は Study の README に書き戻して初めて完了
- **作者の言葉のまま**。STATEMENT と観察の引用は改変しない。AI は構造化のみ行う
- **道具は研究の堆積物**。Instrument を先回りして作り込まない。Study で必要になったものだけが昇格する

## 2. 役割分担

- **人間（作者）** = 目と手と最終判断。視覚で判断する人。造形の良し悪し・実物実験の実行・
  安全に関わる判断・Study の昇格承認は人間のみが行う
- **実装モデル** = 道具づくり。タスク指示書の制約内で技術選定は自由（根拠を README に残す）。
  不明点は仕様を発明せず、質問リストにして人間へ返す
- **観察係 AI** = 言語化・整理・記録・問いの提案。骨子の改訂は意図的に、版と日付を残す

## 3. 作業セッションの型

**開始時**: §0 の読む順に従う。既存の未コミット変更を確認し、無関係なら触らない。

**終了時**（すべて満たして完了）:
1. 対象 Study の README に Observation を追記（見たままを書く。解釈と区別する）
2. manifest.json の revisits に日付と一行を追記
3. 動作確認（ビルドが通る・ブラウザで開ける）。UI の対話性は**実座標のクリック**か
   `document.elementFromPoint` で確かめる — 合成 `element.click()` はヒットテストを迂回するため、
   要素が別の要素に覆われていても成功してしまう（2026-07-10 S2c で実害。作者の実機で全操作不能だった）
4. 変更点と未解決を1段落で人間に報告
5. 公開に影響する変更（`src/`・各 `.html`・依存）があった場合は `npm run deploy` を実行し、
   https://katachi.a-8c3.workers.dev/ で当該変更を確認する。デプロイしない場合は理由を報告する。
   `docs/`・README だけの変更は対象外。実行前に `wrangler whoami` で認証を確認し、未認証なら作者へ依頼する

## 4. Study の形式

`src/studies/<name>/` に自己完結。中身: コード＋`README.md`＋`manifest.json`＋記録置き場（`notes/`）。

README の型: **Question**（この Study は何を知ろうとしているか）／**Setup**（何をどう動かすか）／
**Observation**（見えたこと。日付つき）／**Hypothesis**（そこから立てた仮説）／
**Related**（他 Study・MorphogenesisLab への参照）／**Next**（開いている糸）

**昇格**: 同じ操作を2つ以上の Study で使いたくなったら Library への昇格を人間に提案する。
基準は「安定していて、単体で説明できて、再利用の実需がある」。

## 5. コード原則

- ブラウザで動く。依存は最小（MorphogenesisLab と同水準の軽さ。重装備フレームワーク禁止）
- **場が第一級**。表面（メッシュ）は場からの導出物。操作は「場への書き込み」として実装し、
  **操作の履歴（三つの手のレシピ）を JSON で書き出し・読み込みできる**ことを常に保つ
- すべての数値は書き出せる（観察の記録可能性）。スクリーンショットだけで版が分かる
  （ルート共通 UI ルール: Version と UpdatedAt を表示）
- **余白の色は全 Study 共通のスケール**にする（青=楽 ↔ 赤=限界。一度決めたら変えない。
  変える場合は骨子を改訂して版を残す）
- 速度より正確さ。ただし「こねる」体験は応答性が命なので、精度と応答のトレードオフは
  README に明記して人間に選ばせる

## 6. 安全（このプロジェクト固有・最重要）

- **実物の実験（印刷・破壊・荷重・人に触らせる）は必ず人間が実行する**。AI は手順の提案と記録のみ
- 道具は「安全だ」と断言しない。表示するのは**余白の推定値とその根拠・限界**のみ。
  断定は人間の仕事であり責任である
- 人（特に子供）が触れる実物は、画面の予測に関わらず人間が実物で二重チェックする
- 破壊試験は保護具と周囲の安全を人間が確認してから

## 7. 文書の版管理

- 骨子（RESEARCH.md）の改訂は意図的に行い、版と日付を冒頭に残す。地図の書き直しは失敗ではなく研究の進行
- 実装と文書がずれたら、ずれを放置しない（ルート共通ルール）
- 大きな決定（技術選定・色スケール・ファイル形式）は README か骨子に「理由つきで」残す

## 8. Scope Lock / Stop Rule

- 開始時に今回の Definition of Done（DoD）を3〜7項目で固定し、必要十分な最小変更で満たしたら停止する
- DoDを妨げないscope外の発見は修正せず `FOLLOW-UP` に送る。reviewerも今回のDoDだけを判定する
- `review → fix → review` の修正サイクルは原則2周まで。以後は現状・未解決・次TASK案を報告して停止する
- 指定外のrefactor、cleanup、abstraction、generalization、architecture変更、網羅的edge-case対応を「ついでに」行わない
- 追加修正の前に「これを直さないと今回のDoDを満たせないか？」と判定し、Noなら修正しない
- FOLLOW-UPを残して終了してよい。今回のDoDを満たして止めることを成功とする

## 9. SKIN / Print 固有ルール

- 物理試作とPrint実験を、理論的完全性の追求より優先する。理論上の不足は試作を無効にしない限りresearch findingとする
- Print実験を、無関係なsupport hardeningで止めない。必要なら `FOLLOW-UP` に分離する
- CUDAは明示的な統合タスク以外では隔離し、Print・production・既存CPU経路へ偶発的に結合しない
- FKEIの意味、単位、符号、対応関係を不用意に変えない。変更には明示要件と移行・検証根拠を要する
- immutable print baselineは上書き・再生成せず、比較用の読み取り専用基準として維持する。新結果は別versionまたは別pathへ出す
- reviewではproduction blockerとresearch findingを分離する。要求未達、データ損失、重大な安全問題、production regression、破損・再現不能だけをblockerとする
