# C — SKIN Production UI IA v0A Fix 2 · Current Production Parity Evidence

Date: 2026-09-06
Owner: C SOL
Implementation owner: C LUNA / bounded verification worker
Status: ACTIVE

## Review source

UI checkpoint:

`22ef22bcc327e048ed994c8fa8c3964ad20b324c`

Branch:

`agent/skin-production-ui-ia-v0`

C SOL review found the five-phase UI implementation itself acceptable: FLOW now switches phase-specific Inspector presentation, Permanent Structure and Removable Support are distinct, and current Export is separated from compatibility/legacy material.

The remaining blocker is evidence only.

## Why Fix 2 is required

The branch-local `C_CURRENT.md` points to `docs/evidence/skin-production-v0-geometry-fidelity-fix1/` as deterministic parity evidence.

That package is not evidence for the locked current Production checkpoint used by UI IA. It contains P1/P3 fingerprints such as:

- `90746987...`
- `116c73c6...`

and explicitly states that Support and 3MF export were not touched.

The locked current Production identity for UI IA remains:

- baseline: `349e1a854d7e3699ac29afd167fc22e8131406d7`
- Permanent BODY fingerprint: `c9ed4d69512aa20cb994083239afc5d26ce0402abbdb5a79e2165ca521cdb501`
- supportSource: `current-stage8:sparseResult.graph`
- production 3MF SHA-256 authority: `bbc0af54bb6f038e61f211666a7a4378785cf2b6b106f9771acbbe5c96587e1c`

Therefore Fix 1 is not globally CLOSED until current-baseline BODY / Graph / Support / 3MF parity is recorded explicitly.

## Scope

This is an **evidence-only verification task**.

Do not redesign or further refactor the UI.
Do not change Production algorithms, state, parameters, export semantics, or support semantics.

Preferred outcome: run an existing current-C verification/replay path against the UI checkpoint and record its outputs.

If no existing verification path can emit the required current identities, STOP and report the exact missing harness/evidence path. Do not invent a new geometry/export architecture merely to satisfy reporting.

## Required current-baseline evidence

Verify the UI checkpoint against the locked current Production baseline and record:

- Host: EXACT
- Motifs / transforms: EXACT
- Permanent Graph: EXACT
- Permanent BODY fingerprint: exactly `c9ed4d69512aa20cb994083239afc5d26ce0402abbdb5a79e2165ca521cdb501`
- supportSource: exactly `current-stage8:sparseResult.graph`
- Removable Support artifact/graph: EXACT against the current Production authority
- 3MF Artwork BODY: EXACT
- 3MF Support: EXACT
- production 3MF identity/hash where the existing verification path emits it; expected current authority SHA-256 is `bbc0af54bb6f038e61f211666a7a4378785cf2b6b106f9771acbbe5c96587e1c`
- FIELD vNext semantics: unchanged / PASS
- Output Scale semantics: unchanged

Run deterministic current Production replay twice and record that the two complete result sets are identical.

Do not substitute historical geometry-fidelity P1/P3 fingerprints for the current Production BODY fingerprint.

## Source-change boundary

Expected source-code changes: **NONE**.

Allowed changes:

- evidence files generated by an existing verification path;
- branch-local `docs/status/C_CURRENT.md` factual correction;
- focused verification script invocation output if the script already exists.

If a tiny correction to a verification-only script is genuinely required, stop and report it to C SOL before changing source. No UI or Production source change is authorized by this task.

## Tests / checks

Re-run only what is necessary to prove the checkpoint remains healthy and to produce exact parity evidence. At minimum preserve prior PASS state for:

- relevant UI tests;
- `npm run test:skin-rebuild`;
- typecheck;
- `npm run build`;
- `git diff --check`.

The decisive gate for Fix 2 is the exact current-baseline replay/parity evidence above.

## Checkpoint

If all required evidence is exact:

1. commit evidence/documentation only on `agent/skin-production-ui-ia-v0`;
2. push branch;
3. do not merge;
4. do not deploy;
5. return compact C SOL review handoff.

If the exact evidence cannot be produced with existing verification paths, do not claim PASS. Return the exact blocker and leave source unchanged.

## Done when

UI IA v0A is ready for final C SOL closure only when the already-reviewed UI checkpoint is accompanied by explicit current-baseline BODY / Graph / Support / 3MF exact parity evidence and deterministic replay x2.