# Research Astra — Culprit Branch / Layer-Causal Fix v5

Date: 2026-09-07
Owner: Research SOL
Research worker: Research Astra
Status: READY FOR RESEARCH

## Author decision / visual authority

The current v4 3D direction is **Author-approved as the visual baseline**.

Preserve:

- USAGI host legibility;
- current botanical / plant-like reading;
- sparse volumetric structure;
- current multi-depth occupation and Void / openness character;
- exact authoritative A2 motif construction;
- uniform A2 motif scale `0.5x` relative to the original 2000% A2 reference;
- motif count `512`;
- current D1 material ancestry `512/512` and intended single connected artwork body.

Do not restart broad aesthetic exploration. Do not search for a new family of forms.

Read together with:

- `docs/tasks/RESEARCH_ASTRA_MATERIAL_ANCESTRY_PRINTABILITY_FIX_V3.md`
- prior Research package `PRINTABLE_FRONTIER_ATTACHMENT_MULTIPLICITY_V4`
- `docs/observations/AUTHOR_OBSERVATION_SHAPE_ALGORITHM_BOTANICAL_CONTROL_2026-09-07.md`
- current `docs/status/AB_CURRENT.md`
- current `docs/status/C_CURRENT.md`

## v4 accepted evidence / current problem

Use v4 as the comparison authority, not the older v3 geometry.

Accepted current evidence includes:

- D0 member count: `211`;
- D0 island births: `38`;
- D0 Host-depth >= 2 mm births: `23`;
- D0 recursively reached internal nodes: `75 / 96`;
- D0 Bambu Support-OFF floating warning: still present;
- D0 Support-ON diagnostic: substantial Host-deep Support dependence remains;
- D1 exact A2: `0.5x / 512`;
- D1 exact material ancestry: `512 / 512`;
- D1 intended artwork body: `1 connected positive body`;
- v4 visual direction: Author PASS / preserve.

v4 showed that monotonic-Z correction can reduce island births while preserving appearance, but it also showed that simply flattening local valleys is not sufficient: uncovered / overhang diagnostics can regress and Bambu floating remains.

## Core hypothesis

Do not treat the remaining `38` D0 island births as a global structure problem.

Instead:

> Attribute each remaining layer-birth event to the smallest responsible Permanent member / junction set, identify the minimum set of culprit branches that explains the unresolved events, and replace only those local paths with alternatives that are generated from an already-printable frontier and satisfy layer-causal manufacturing constraints.

The next step is therefore:

```text
remaining island birth
        -> culprit member / junction attribution
        -> printable-frontier parent set
        -> layer-causal local replacement
        -> bounded junction / branch-order / anastomosis update
        -> re-evaluate D0
```

The goal is **not** to make the whole network more vertical, denser, or thicker.

## Main candidate multiplicity

For this v5 main candidate:

- fix `attachmentMultiplicityTarget = 1`;
- do not continue multiplicity 2 / 3 exploration as part of the main geometry search;
- retain v4 multiplicity evidence as future robustness / Product-art research;
- do not use extra attachment multiplicity as a shortcut for printability.

If multiplicity-related data structures are already present, preserve them and record them, but do not let them expand this task.

## Stage 1 — Culprit attribution

Before changing geometry, produce a machine-readable attribution of the `38` remaining D0 island-birth events.

For every event, record at minimum:

- event ID;
- build height / layer region;
- Host depth;
- responsible mesh region;
- nearest / contributing Permanent member IDs;
- nearest junction / node IDs where applicable;
- whether the event is caused primarily by:
  - local valley / downward turn;
  - excessive lateral run from previously established material;
  - isolated branch start;
  - junction order / chronology;
  - unsupported emergence segment;
  - other explicitly documented cause;
- confidence / ambiguity if multiple members could own the event.

Then cluster events by culprit member / junction and rank the **minimum culprit set** that explains the largest share of unresolved births.

Do not change geometry until this attribution is saved.

## Stage 2 — Layer-causal replacement

Only modify culprit paths needed to address the unresolved D0 births.

Allowed operations:

- replace a culprit branch with another candidate from an already-established printable frontier;
- move a local junction within a bounded neighborhood;
- change local branch order / chronology so the parent material exists first;
- add bounded anastomosis / reconnection to an already-established lower branch where it is part of the Permanent artwork language;
- relax a local endpoint when necessary to obtain a truly printable path, provided the approved visual direction remains intact;
- adjust local curvature / tangent direction;
- use bounded local radius adaptation only if direct manufacturing evidence justifies it.

The replacement must be generated / reproducible. Blender-only hand editing may be used for inspection, not as the sole authority.

### Layer-causal condition

Do not invent an arbitrary universal angle threshold if the current Bambu / layer evidence does not support one.

Instead, derive and document the local admissibility rule from the same locked build orientation / inspection profile and existing layer diagnostics. The rule may combine:

- previously established material proximity;
- local rise / lateral run;
- supported bridge span;
- member radius;
- local layer contact;
- branch chronology;
- existing neighboring Permanent material.

Store the rule and its parameters explicitly so it can later become a fabrication constraint rather than a hidden aesthetic edit.

## Protected visual boundary

The v4 Author-approved 3D appearance is the baseline.

Do not solve D0 by:

- broad densification;
- adding many new members;
- global radius increase;
- near-vertical collapse;
- generic lattice / truss substitution;
- Host shell tracing;
- reducing motif count;
- changing exact A2 motif geometry;
- changing motif scale from `0.5x`;
- arbitrary decorative branches;
- changing the design merely to satisfy one diagnostic metric.

Report before / after:

- member count;
- nominal / union volume;
- radius distribution;
- orientation distribution;
- Host-depth distribution;
- D0 / D1 projected openness;
- front / oblique / reverse comparison;
- orbit / multi-angle comparison.

If eliminating the remaining floating condition requires a visible departure from the approved v4 direction, STOP with `RESEARCH STRUCTURE FIX REQUIRED` and show the conflict instead of silently changing the artwork.

## D0 gate — Permanent Structure first

Rebuild and inspect D0 after the culprit-targeted fix.

Required evidence:

- connected positive Permanent body;
- watertight / winding / manifold audit;
- island births and internal births;
- recursive reachability / uncovered diagnostics;
- overhang / layer-causal diagnostics;
- Bambu Studio Support OFF inspection in the locked 1000% build orientation;
- D0 Support-ON diagnostic probe only as evidence of residual dependence, not as a new gate definition;
- map from old `38` events to:
  - resolved;
  - moved / transformed;
  - still unresolved;
  - newly introduced.

### D0 target for physical-gate eligibility

Do not advance merely because island count is lower.

For `READY FOR AUTHOR PHYSICAL GATE`, require:

- no unresolved Bambu floating-regions warning for D0 under the documented inspection profile;
- no identified inaccessible internal removable-Support dependency for D0;
- any remaining layer-diagnostic birth must be explicitly reconciled with actual slicer behavior and shown not to require inaccessible internal Support;
- no unacceptable visual drift from the v4 Author baseline.

If these cannot be achieved, return `RESEARCH STRUCTURE FIX REQUIRED`.

## D1 gate — keep exact motif / material ancestry

After D0 stabilizes, rebuild D1 with:

- exact A2 construction;
- uniform `0.5x`;
- count `512`;
- multiplicity main target `1`;
- algorithmic ancestry retained;
- material ancestry retained `512/512` unless an explicitly documented failure forces STOP;
- intended artwork body remains materially connected.

Re-check:

- positive body/component count;
- disconnected motif groups;
- ancestry validity;
- local emergence connectivity after any D0 culprit replacement;
- D1 island / reachability behavior;
- front / oblique / reverse and orbit against v4 D1;
- projected openness / Host legibility / botanical reading.

Do not repair D1 by adding 512 post-hoc spokes or a new support cage.

## D2 / D3 — only after D0 / D1

Continue the same fabrication hierarchy only after Permanent decisions stabilize:

```text
D0 = Permanent Structure only
D1 = D0 + exact A2 motifs 0.5x / 512
D2 = D1 + residual authored Outside->Outside removable Support
D3 = D2 + residual slicer Support fallback
```

For D2:

- use only true residual unsupported demand;
- preserve anchor / target / gap / clearance semantics;
- keep authored Support externally accessible / removable;
- do not route authored removable Support through inaccessible interior Void;
- distinguish bed-anchored vs body-anchored results;
- do not optimize D2 independently of D0 / D1.

For D3:

- remeasure actual Support path / volume / Host depth;
- compare against v4;
- warning disappearance alone is not PASS;
- explicitly report how much fallback still enters Host-deep / potentially inaccessible regions.

## Future SKIN translation boundary

Do not implement Production or UI in v5.

However, preserve the distinction between:

### Artistic-expression parameters
- Host fidelity;
- growth / botanical expression;
- branch wandering;
- anastomosis expression;
- Void articulation;
- emergence timing / direction;
- motif orientation / variation.

### Fabrication constraints
- printable-frontier eligibility;
- layer-causal admissibility;
- minimum radius;
- connectivity;
- maximum unsupported local span;
- residual Support policy.

Any new value introduced by v5 must be recorded in one of these groups instead of being hidden in code.

## Required Research artifacts

Produce immediately inspectable artifacts:

- separated `.blend` with at least:
  - `HOST_REF`
  - `VOID_GUIDES`
  - `PERMANENT_STRUCTURE`
  - `A2_MOTIFS`
  - `AUTHORED_REMOVABLE_SUPPORT`
- D0 `.3mf`;
- D1 `.3mf`;
- D2 `.3mf` if D2 is reached;
- D3 Bambu comparison `.3mf` if D3 is reached;
- culprit-event attribution visualization;
- culprit-member / junction ranking;
- before / after D0 front / oblique / reverse;
- before / after D1 front / oblique / reverse;
- D1 orbit / multi-angle comparison;
- unresolved-demand map;
- D0 Support-OFF Bambu evidence;
- D0 Support-ON residual-dependence probe;
- D3 Support-location evidence if reached;
- machine-readable member changes / ancestry / layer-causal rule / demand classification / Support assignment;
- manifest with hashes / provenance;
- exact Bambu profile / nozzle / material / orientation assumptions.

## Review questions

Research SOL / Author must be able to answer:

1. Which Permanent members / junctions caused the remaining 38 v4 D0 island births?
2. How many culprit members explain most of the unresolved births?
3. Were only those local paths changed?
4. Did the fix improve actual D0 slicer behavior rather than only one proxy metric?
5. Is the v4 USAGI + botanical appearance still intact?
6. Is exact A2 0.5x / 512 and material ancestry 512/512 still intact?
7. How much D0 internal Support dependence remains?
8. After D0 / D1, what true residual remains for D2 and D3?
9. Is the system finally eligible for Author physical printing, or still Research-only?

## Outcome labels

Use only:

- `RESEARCH STRUCTURE FIX REQUIRED`
- `DIGITAL D0 PRINT PREFLIGHT PASS — PHYSICAL UNPROVEN`
- `RESEARCH FABRICATION SYSTEM READY — PHYSICAL UNPROVEN`
- `READY FOR AUTHOR PHYSICAL GATE`
- `PHYSICAL STRUCTURE PRINT PASS`
- `PHYSICAL SYSTEM PRINT PASS`
- `HOLD`

## HOLD / DO NOT DO

- no Production C / AB modification;
- no A2 baseline mutation;
- no Viewer expansion;
- no broad new aesthetic exploration;
- no multiplicity > 1 main-candidate exploration;
- no motif approximation / count reduction;
- no global densification / thickening / verticalization shortcut;
- no generic support lattice replacement;
- no hidden manual Blender geometry authority;
- no inaccessible internal authored removable Support;
- no Production translation;
- no merge / deploy;
- no new Team creation.

STOP at the bounded Research package or Author physical-gate boundary and return evidence for Research SOL review.
