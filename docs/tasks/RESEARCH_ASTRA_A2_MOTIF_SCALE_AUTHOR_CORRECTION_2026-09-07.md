# Research Astra — Author correction for A2 motif scale / count

Date: 2026-09-07
Owner: Research SOL / Author
Status: AUTHOR CORRECTION — applies to `RESEARCH_ASTRA_PRINT_PROVEN_A2_MOTIF_INTEGRATED_V2.md`

## Authority / precedence

Read and execute together with:

- `docs/tasks/RESEARCH_ASTRA_PRINT_PROVEN_A2_MOTIF_INTEGRATED_V2.md`

Where this note adds or resolves an ambiguity, **this author correction takes precedence**.

## Motif correction

The A2 motif is the exact authoritative A2 construction already recovered by Research Astra. Do not approximate or redesign it.

For the new 1000% Host comparison:

- motif geometry / construction: exact A2 authority;
- motif global scale relative to the original A2 2000% reference: **0.5x uniformly**;
- motif count: **512**;
- preserve the A2 motif visual language and local proportional construction under the uniform 0.5x scaling;
- do not replace the motif with the simplified Integrated-v1 64-loop representation.

The reason for 0.5x is comparison scale normalization: the A2 reference Host was 2000%, while the current Integrated / D candidate Host is 1000%.

Record the exact scaling transform and provenance so A2-vs-D comparison remains auditable.

## Visual output required even while structure gate is unresolved

The current Permanent Structure direction / density / overall atmosphere is author-approved enough to continue visual review.

Therefore, in addition to structure-only evidence, produce **visual-review-only motif-included renders** using:

- current structure candidate;
- exact A2 motif construction;
- uniform 0.5x motif scale;
- 512 motifs.

At minimum provide motif-included:

- front;
- oblique;
- reverse;
- multi-angle orbit / comparable sheet where practical.

These renders are **not printability proof** and must be labelled as visual-review evidence if D0 structure still fails the manufacturing gate.

Do not let the unresolved D0 structure gate prevent the author from seeing the motif-included appearance, provided the output is clearly separated from manufacturing claims.

## Protected structure characteristics

The author currently approves the general structure atmosphere from the latest preflight enough to preserve it as the visual reference.

Continue to protect:

- current sparse structural density;
- current non-uniform / non-vertical directionality;
- multi-depth occupation;
- perceptual openness / interwoven Void ambition;
- distinction from conventional slicer support / dense infill / generic truss.

Do not solve printability by broad densification, global thickening, or near-vertical collapse.

## Fabrication hierarchy remains unchanged

Continue to use the v2 hierarchy:

```text
D0 = Permanent Structure only
D1 = D0 + exact A2 motifs at 0.5x, count 512
D2 = D1 + residual authored Outside->Outside removable Support
D3 = D2 + residual slicer Support fallback
```

Use the same unsupported-demand analysis to decide what belongs in Permanent Structure, what remains residual authored removable Support, and what falls through to slicer fallback.

Do not independently optimize Outside->Outside Support before Permanent Structure decisions stabilize.

## Claim boundary

- Structure-only printability and motif-included visual quality are separate axes.
- Motif-included renders may proceed for Author Review even if D0 remains `RESEARCH STRUCTURE FIX REQUIRED`.
- Motif-included manufacturing / printability must not be claimed from visuals.
- Production C / AB remains unchanged.

STOP according to the parent v2 task gate rules after the bounded Research package or physical gate boundary is reached.
