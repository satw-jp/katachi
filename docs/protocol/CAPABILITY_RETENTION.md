# Capability Retention Reference

For systems with historical branches or multiple representations:

```text
historical implementation exists != current production retains it
```

Use the capability ledger and current evidence. Do not bulk-merge historical branches merely to recover features. Restore only capabilities still required and semantically valid in the current architecture. Record whether a capability is current, historical, compatibility-only, or unverified.
